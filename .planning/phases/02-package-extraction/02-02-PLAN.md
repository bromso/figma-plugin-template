---
phase: 02-package-extraction
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/figma-plugin/vite.config.ui.ts
  - apps/figma-plugin/vite.config.plugin.ts
  - apps/figma-plugin/figma.manifest.ts
  - apps/figma-plugin/src/plugin/plugin.ts
  - apps/figma-plugin/src/plugin/plugin.network.ts
  - packages/ui/src/main.tsx
  - packages/ui/src/app.tsx
  - packages/ui/src/app.network.tsx
  - packages/ui/src/index.html
  - packages/ui/src/vite-env.d.ts
  - packages/ui/src/components/Button.tsx
  - packages/ui/src/styles/main.scss
  - packages/ui/src/utils/classes.util.ts
  - packages/ui/src/assets/figma.png
  - packages/ui/src/assets/react.svg
  - packages/ui/src/assets/vite.svg
  - packages/ui/src/assets/fonts/Alkatra.ttf
  - packages/ui/src/components/Button.module.scss
autonomous: true
requirements: [MONO-01, MONO-03, MONO-04, MONO-05]

must_haves:
  truths:
    - "All source files are in their correct package directories (no files remain in root src/)"
    - "All imports use @repo/common/ for cross-package references (no @common/ or @ui/ path aliases)"
    - "Within-package imports use relative paths (no @ui/ alias within packages/ui/)"
    - "Vite configs resolve paths correctly relative to apps/figma-plugin/"
    - "Root tsconfig.json, tsconfig.node.json, vite configs, and figma.manifest.ts are deleted"
    - "TypeScript type-check passes across all packages"
  artifacts:
    - path: "apps/figma-plugin/vite.config.ui.ts"
      provides: "UI Vite build config with updated root pointing to ../../packages/ui/src"
      contains: "packages/ui/src"
    - path: "apps/figma-plugin/vite.config.plugin.ts"
      provides: "Plugin Vite build config with updated input path"
      contains: "src/plugin/plugin.ts"
    - path: "apps/figma-plugin/src/plugin/plugin.ts"
      provides: "Plugin entry point with @repo/common imports"
      contains: "@repo/common"
    - path: "packages/ui/src/app.tsx"
      provides: "React app component with relative imports and @repo/common"
    - path: "packages/ui/src/main.tsx"
      provides: "React entry point with @repo/common import"
      contains: "@repo/common"
  key_links:
    - from: "apps/figma-plugin/vite.config.ui.ts"
      to: "packages/ui/src"
      via: "Vite root config"
      pattern: "path\\.resolve.*packages/ui/src"
    - from: "packages/ui/src/app.network.tsx"
      to: "packages/common/src/networkSides.ts"
      via: "@repo/common/networkSides import"
      pattern: 'from "@repo/common/networkSides"'
    - from: "apps/figma-plugin/src/plugin/plugin.ts"
      to: "packages/common/src/networkSides.ts"
      via: "@repo/common/networkSides import"
      pattern: 'from "@repo/common/networkSides"'
---

<objective>
Move all source files from root src/ into their correct package directories, update all import paths from path aliases to workspace package imports, update Vite configs for new file locations, and delete obsolete root files.

Purpose: Complete the physical migration so the monorepo structure is fully functional with correct cross-package resolution.
Output: All source code in package directories, all imports updated, Vite builds work from apps/figma-plugin/, root src/ deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-extraction/02-RESEARCH.md
@.planning/phases/02-package-extraction/02-01-SUMMARY.md

@vite.config.ui.ts (current, to be moved and updated)
@vite.config.plugin.ts (current, to be moved and updated)
@figma.manifest.ts (current, to be moved)
@src/plugin/plugin.ts
@src/plugin/plugin.network.ts
@src/ui/main.tsx
@src/ui/app.tsx
@src/ui/app.network.tsx
@src/ui/components/Button.tsx
</context>

<interfaces>
<!-- Import map: old path alias imports to new workspace imports -->
<!-- Cross-package (use @repo/common/): -->
<!--   @common/networkSides  -->  @repo/common/networkSides
<!--   Files: app.network.tsx, plugin.network.ts, plugin.ts, main.tsx, app.tsx -->

<!-- Within packages/ui (use relative): -->
<!--   @ui/app.network      -->  ./app.network
<!--   @ui/components/Button -->  ./components/Button
<!--   @ui/assets/figma.png  -->  ./assets/figma.png
<!--   @ui/assets/react.svg  -->  ./assets/react.svg?component
<!--   @ui/assets/vite.svg   -->  ./assets/vite.svg?url
<!--   @ui/styles/main.scss  -->  ./styles/main.scss
<!--   @ui/utils/classes.util --> ./utils/classes.util
<!--   Files: main.tsx, app.tsx, Button.tsx -->

<!-- Within apps/figma-plugin (use relative): -->
<!--   @plugin/plugin.network --> ./plugin.network
<!--   File: plugin.ts -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move UI and plugin source files to package directories</name>
  <files>
    packages/ui/src/main.tsx,
    packages/ui/src/app.tsx,
    packages/ui/src/app.network.tsx,
    packages/ui/src/index.html,
    packages/ui/src/vite-env.d.ts,
    packages/ui/src/components/Button.tsx,
    packages/ui/src/components/Button.module.scss,
    packages/ui/src/utils/classes.util.ts,
    packages/ui/src/styles/main.scss,
    packages/ui/src/styles/base/_base.scss,
    packages/ui/src/styles/base/_fonts.scss,
    packages/ui/src/styles/base/_typography.scss,
    packages/ui/src/styles/abstracts/_variables.scss,
    packages/ui/src/styles/vendors/_normalize.scss,
    packages/ui/src/styles/pages/_home.scss,
    packages/ui/src/assets/figma.png,
    packages/ui/src/assets/react.svg,
    packages/ui/src/assets/vite.svg,
    packages/ui/src/assets/fonts/Alkatra.ttf,
    apps/figma-plugin/src/plugin/plugin.ts,
    apps/figma-plugin/src/plugin/plugin.network.ts,
    apps/figma-plugin/figma.manifest.ts,
    apps/figma-plugin/vite.config.ui.ts,
    apps/figma-plugin/vite.config.plugin.ts
  </files>
  <read_first>
    src/ui/main.tsx,
    src/ui/app.tsx,
    src/ui/app.network.tsx,
    src/ui/index.html,
    src/ui/components/Button.tsx,
    src/ui/utils/classes.util.ts,
    src/plugin/plugin.ts,
    src/plugin/plugin.network.ts,
    vite.config.ui.ts,
    vite.config.plugin.ts,
    figma.manifest.ts
  </read_first>
  <action>
Move files using `git mv` to preserve history. Execute these commands in order:

```bash
# Create target directories
mkdir -p packages/ui/src/components
mkdir -p packages/ui/src/utils
mkdir -p packages/ui/src/styles/base
mkdir -p packages/ui/src/styles/abstracts
mkdir -p packages/ui/src/styles/vendors
mkdir -p packages/ui/src/styles/pages
mkdir -p packages/ui/src/assets/fonts
mkdir -p apps/figma-plugin/src/plugin

# Move UI source files
git mv src/ui/main.tsx packages/ui/src/main.tsx
git mv src/ui/app.tsx packages/ui/src/app.tsx
git mv src/ui/app.network.tsx packages/ui/src/app.network.tsx
git mv src/ui/index.html packages/ui/src/index.html
git mv src/ui/vite-env.d.ts packages/ui/src/vite-env.d.ts
git mv src/ui/components/Button.tsx packages/ui/src/components/Button.tsx
git mv src/ui/components/Button.module.scss packages/ui/src/components/Button.module.scss
git mv src/ui/utils/classes.util.ts packages/ui/src/utils/classes.util.ts
git mv src/ui/styles/main.scss packages/ui/src/styles/main.scss
git mv src/ui/styles/base/_base.scss packages/ui/src/styles/base/_base.scss
git mv src/ui/styles/base/_fonts.scss packages/ui/src/styles/base/_fonts.scss
git mv src/ui/styles/base/_typography.scss packages/ui/src/styles/base/_typography.scss
git mv src/ui/styles/abstracts/_variables.scss packages/ui/src/styles/abstracts/_variables.scss
git mv src/ui/styles/vendors/_normalize.scss packages/ui/src/styles/vendors/_normalize.scss
git mv src/ui/styles/pages/_home.scss packages/ui/src/styles/pages/_home.scss
git mv src/ui/assets/figma.png packages/ui/src/assets/figma.png
git mv src/ui/assets/react.svg packages/ui/src/assets/react.svg
git mv src/ui/assets/vite.svg packages/ui/src/assets/vite.svg
git mv src/ui/assets/fonts/Alkatra.ttf packages/ui/src/assets/fonts/Alkatra.ttf

# Move plugin source files
git mv src/plugin/plugin.ts apps/figma-plugin/src/plugin/plugin.ts
git mv src/plugin/plugin.network.ts apps/figma-plugin/src/plugin/plugin.network.ts

# Move build config files to apps/figma-plugin/
git mv vite.config.ui.ts apps/figma-plugin/vite.config.ui.ts
git mv vite.config.plugin.ts apps/figma-plugin/vite.config.plugin.ts
git mv figma.manifest.ts apps/figma-plugin/figma.manifest.ts
```

After all moves, delete the now-empty `src/` directory (it should only contain `src/common/networkSides.ts` which was already copied to packages/common in Plan 01; delete the original):
```bash
rm -rf src/
```

Also delete the obsolete root tsconfig files (replaced by per-package tsconfigs in Plan 01):
```bash
git rm tsconfig.json
git rm tsconfig.node.json
```

Do NOT modify file contents yet -- just move files. Import path updates happen in Task 2.
  </action>
  <verify>
    <automated>ls packages/ui/src/main.tsx packages/ui/src/app.tsx apps/figma-plugin/src/plugin/plugin.ts apps/figma-plugin/vite.config.ui.ts apps/figma-plugin/figma.manifest.ts && test ! -d src && test ! -f tsconfig.json && test ! -f tsconfig.node.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/ contains main.tsx, app.tsx, app.network.tsx, index.html, vite-env.d.ts
    - packages/ui/src/components/ contains Button.tsx, Button.module.scss
    - packages/ui/src/utils/ contains classes.util.ts
    - packages/ui/src/styles/ contains main.scss and all subdirectory SCSS files
    - packages/ui/src/assets/ contains figma.png, react.svg, vite.svg, fonts/Alkatra.ttf
    - apps/figma-plugin/src/plugin/ contains plugin.ts, plugin.network.ts
    - apps/figma-plugin/ contains vite.config.ui.ts, vite.config.plugin.ts, figma.manifest.ts
    - Root src/ directory no longer exists
    - Root tsconfig.json and tsconfig.node.json no longer exist
  </acceptance_criteria>
  <done>All source files physically moved to correct package directories. Root src/, tsconfig.json, tsconfig.node.json deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Update import paths and Vite configs for workspace resolution</name>
  <files>
    packages/ui/src/main.tsx,
    packages/ui/src/app.tsx,
    packages/ui/src/app.network.tsx,
    packages/ui/src/components/Button.tsx,
    apps/figma-plugin/src/plugin/plugin.ts,
    apps/figma-plugin/src/plugin/plugin.network.ts,
    apps/figma-plugin/vite.config.ui.ts,
    apps/figma-plugin/vite.config.plugin.ts
  </files>
  <read_first>
    packages/ui/src/main.tsx,
    packages/ui/src/app.tsx,
    packages/ui/src/app.network.tsx,
    packages/ui/src/components/Button.tsx,
    apps/figma-plugin/src/plugin/plugin.ts,
    apps/figma-plugin/src/plugin/plugin.network.ts,
    apps/figma-plugin/vite.config.ui.ts,
    apps/figma-plugin/vite.config.plugin.ts
  </read_first>
  <action>
Update all import paths according to this exact mapping. Every old alias import must be replaced:

**packages/ui/src/main.tsx:**
- `import { PLUGIN, UI } from "@common/networkSides"` --> `import { PLUGIN, UI } from "@repo/common/networkSides"`
- `import { UI_CHANNEL } from "@ui/app.network"` --> `import { UI_CHANNEL } from "./app.network"`
- `import "@ui/styles/main.scss"` (if present) --> `import "./styles/main.scss"` (check actual file content)

**packages/ui/src/app.tsx:**
- `import { PLUGIN } from "@common/networkSides"` --> `import { PLUGIN } from "@repo/common/networkSides"`
- `import { UI_CHANNEL } from "@ui/app.network"` --> `import { UI_CHANNEL } from "./app.network"`
- `import { Button } from "@ui/components/Button"` --> `import { Button } from "./components/Button"`
- `import figmaLogo from "@ui/assets/figma.png"` --> `import figmaLogo from "./assets/figma.png"`
- `import ReactLogo from "@ui/assets/react.svg?component"` --> `import ReactLogo from "./assets/react.svg?component"`
- `import viteLogo from "@ui/assets/vite.svg?url"` --> `import viteLogo from "./assets/vite.svg?url"`
- `import "@ui/styles/main.scss"` --> `import "./styles/main.scss"`

**packages/ui/src/app.network.tsx:**
- `import { PLUGIN, UI } from "@common/networkSides"` --> `import { PLUGIN, UI } from "@repo/common/networkSides"`

**packages/ui/src/components/Button.tsx:**
- `import { classes } from "@ui/utils/classes.util"` --> `import { classes } from "../utils/classes.util"`

**apps/figma-plugin/src/plugin/plugin.ts:**
- `import { PLUGIN, UI } from "@common/networkSides"` --> `import { PLUGIN, UI } from "@repo/common/networkSides"`
- `import { PLUGIN_CHANNEL } from "@plugin/plugin.network"` --> `import { PLUGIN_CHANNEL } from "./plugin.network"`

**apps/figma-plugin/src/plugin/plugin.network.ts:**
- `import { PLUGIN, UI } from "@common/networkSides"` --> `import { PLUGIN, UI } from "@repo/common/networkSides"`

**apps/figma-plugin/vite.config.ui.ts:**
Replace the entire file content with:
```typescript
import { defineConfig } from "vite";
import path from "node:path";
import { viteSingleFile } from "vite-plugin-singlefile";
import react from "@vitejs/plugin-react";
import richSvg from "vite-plugin-react-rich-svg";
import postcssUrl from "postcss-url";

export default defineConfig(({ mode }) => ({
  plugins: [react(), richSvg(), viteSingleFile()],
  root: path.resolve(__dirname, "../../packages/ui/src"),
  build: {
    minify: mode === "production",
    cssMinify: mode === "production",
    sourcemap: mode !== "production" ? "inline" : false,
    emptyOutDir: false,
    outDir: path.resolve(__dirname, "dist"),
    rollupOptions: {
      input: path.resolve(__dirname, "../../packages/ui/src/index.html"),
    },
  },
  css: {
    postcss: {
      plugins: [postcssUrl({ url: "inline" })],
    },
    preprocessorOptions: {
      scss: {
        api: "modern-compiler",
      },
    },
  },
}));
```
Key changes: `root` points to `../../packages/ui/src`, `outDir` uses `__dirname` for stability, `resolve.alias` section is REMOVED entirely (workspace symlinks handle @repo/* resolution), `rollupOptions.input` uses absolute path from `__dirname`.

**apps/figma-plugin/vite.config.plugin.ts:**
Replace the entire file content with:
```typescript
import path from "node:path";
import { defineConfig } from "vite";
import generateFile from "vite-plugin-generate-file";
import { viteSingleFile } from "vite-plugin-singlefile";
import figmaManifest from "./figma.manifest";

export default defineConfig(({ mode }) => ({
  plugins: [
    viteSingleFile(),
    generateFile({
      type: "json",
      output: "./manifest.json",
      data: figmaManifest,
    }),
  ],
  build: {
    minify: mode === "production",
    sourcemap: mode !== "production" ? "inline" : false,
    target: "es2017",
    emptyOutDir: false,
    outDir: path.resolve(__dirname, "dist"),
    rollupOptions: {
      input: path.resolve(__dirname, "src/plugin/plugin.ts"),
      output: {
        entryFileNames: "plugin.js",
      },
    },
  },
  resolve: {
    alias: {
      "@plugin": path.resolve(__dirname, "src/plugin"),
    },
  },
}));
```
Key changes: `outDir` and `input` use `__dirname` for stability, `@common` alias REMOVED (workspace symlink handles it), `@plugin` alias KEPT (points to local src/plugin/ within apps/figma-plugin).

After all edits, run `bun install` to ensure workspace is clean, then verify:
```bash
cd apps/figma-plugin && bun run types
```
This runs `tsc --noEmit` which verifies TypeScript resolves all cross-package imports correctly.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && grep -r "@common/" packages/ apps/ --include="*.ts" --include="*.tsx" | grep -v node_modules; test $? -eq 1 && grep -r '"@ui/' packages/ apps/ --include="*.ts" --include="*.tsx" | grep -v node_modules; test $? -eq 1 && grep '@repo/common/networkSides' packages/ui/src/app.network.tsx && grep '@repo/common/networkSides' apps/figma-plugin/src/plugin/plugin.ts && grep 'packages/ui/src' apps/figma-plugin/vite.config.ui.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Zero files contain `@common/` imports (grep for `@common/` in packages/ and apps/ returns no results)
    - Zero files contain `@ui/` imports (grep for `"@ui/` in packages/ and apps/ returns no results)
    - packages/ui/src/app.network.tsx contains `from "@repo/common/networkSides"`
    - packages/ui/src/app.tsx contains `from "./app.network"` and `from "./components/Button"` and `from "@repo/common/networkSides"`
    - packages/ui/src/main.tsx contains `from "@repo/common/networkSides"` and `from "./app.network"`
    - packages/ui/src/components/Button.tsx contains `from "../utils/classes.util"`
    - apps/figma-plugin/src/plugin/plugin.ts contains `from "@repo/common/networkSides"` and `from "./plugin.network"`
    - apps/figma-plugin/vite.config.ui.ts contains `path.resolve(__dirname, "../../packages/ui/src")` and does NOT contain `resolve.alias`
    - apps/figma-plugin/vite.config.plugin.ts does NOT contain `@common` alias
    - `cd apps/figma-plugin && bun run types` exits with code 0 (TypeScript resolves all imports)
  </acceptance_criteria>
  <done>All import paths migrated from path aliases to workspace package imports. Vite configs updated for new directory structure. TypeScript type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure file migration within a local dev template. No runtime behavior changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | Vite config path resolution | accept | Paths are verified by `bun run types` and build. No external input involved. |
</threat_model>

<verification>
After both tasks complete, run the full Phase 2 gate verification:

```bash
# 1. Directory structure
ls apps/figma-plugin/package.json packages/common/package.json packages/ui/package.json

# 2. No old src/ directory
test ! -d src && echo "src/ removed"

# 3. No old path alias imports remain
grep -r "@common/" packages/ apps/ --include="*.ts" --include="*.tsx" | grep -v node_modules || echo "No @common/ aliases"
grep -r '"@ui/' packages/ apps/ --include="*.ts" --include="*.tsx" | grep -v node_modules || echo "No @ui/ aliases"

# 4. Workspace symlinks
ls -la node_modules/@repo/common node_modules/@repo/ui

# 5. TypeScript type-check
cd apps/figma-plugin && bun run types

# 6. @repo/ namespace
grep '"@repo/' packages/common/package.json packages/ui/package.json apps/figma-plugin/package.json
```
</verification>

<success_criteria>
- All source files moved from root src/ to correct package directories
- Root src/, tsconfig.json, tsconfig.node.json, vite.config.ui.ts, vite.config.plugin.ts, figma.manifest.ts are deleted
- All @common/ imports replaced with @repo/common/ workspace imports
- All @ui/ imports within packages/ui/ replaced with relative imports
- All @plugin/ imports within apps/figma-plugin/ replaced with relative imports
- Vite configs updated with __dirname-based paths and resolve.alias cleaned up
- `bun run types` passes in apps/figma-plugin (TypeScript resolves all workspace imports)
- No stale references to old path aliases remain anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/02-package-extraction/02-02-SUMMARY.md`
</output>
