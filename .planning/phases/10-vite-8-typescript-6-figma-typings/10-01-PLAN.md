---
phase: 10-vite-8-typescript-6-figma-typings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/figma-plugin/package.json
  - apps/figma-plugin/vite.config.plugin.ts
  - apps/figma-plugin/vite.config.ui.ts
  - apps/figma-plugin/tsconfig.json
  - apps/figma-plugin/tsconfig.node.json
  - bun.lock
autonomous: true
requirements: [BUILD-01, BUILD-02, FIG-01]

must_haves:
  truths:
    - "Vite 8.x is installed in apps/figma-plugin and both Vite configs use rolldownOptions (no deprecation warnings)"
    - "TypeScript 6.x is installed and both tsconfigs have explicit types arrays and no deprecated moduleResolution"
    - "@figma/plugin-typings is at 1.124+ in package.json (not pinned to ^1.83.0)"
    - "bun run build completes without errors"
    - "bun run --filter @repo/figma-plugin types passes with no type errors"
  artifacts:
    - path: "apps/figma-plugin/package.json"
      provides: "Updated dependency versions"
      contains: "\"vite\": \"^8.0.0\""
    - path: "apps/figma-plugin/vite.config.plugin.ts"
      provides: "Vite 8 compatible plugin config"
      contains: "rolldownOptions"
    - path: "apps/figma-plugin/vite.config.ui.ts"
      provides: "Vite 8 compatible UI config"
      contains: "rolldownOptions"
    - path: "apps/figma-plugin/tsconfig.json"
      provides: "TS 6 compatible main tsconfig"
      contains: "\"types\": [\"node\"]"
    - path: "apps/figma-plugin/tsconfig.node.json"
      provides: "TS 6 compatible node tsconfig"
      contains: "\"moduleResolution\": \"bundler\""
  key_links:
    - from: "apps/figma-plugin/vite.config.plugin.ts"
      to: "vite@8.x"
      via: "defineConfig import"
      pattern: "rolldownOptions"
    - from: "apps/figma-plugin/tsconfig.json"
      to: "@figma/plugin-typings"
      via: "typeRoots"
      pattern: "typeRoots.*@figma"
---

<objective>
Upgrade Vite 6 to 8, TypeScript 5.3 to 6.0, @vitejs/plugin-react 4 to 6, @figma/plugin-typings to 1.124, and @types/node to 24+ in apps/figma-plugin. Update both Vite configs to use rolldownOptions. Update both tsconfigs for TypeScript 6 compatibility.

Purpose: Establish the current build toolchain foundation that all subsequent v1.2 phases depend on.
Output: Updated package.json, two Vite configs, two tsconfigs, and a working build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vite-8-typescript-6-figma-typings/10-RESEARCH.md

@apps/figma-plugin/package.json
@apps/figma-plugin/vite.config.plugin.ts
@apps/figma-plugin/vite.config.ui.ts
@apps/figma-plugin/tsconfig.json
@apps/figma-plugin/tsconfig.node.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade dependency versions in package.json and install</name>
  <files>apps/figma-plugin/package.json, bun.lock</files>
  <read_first>apps/figma-plugin/package.json, apps/storybook/package.json</read_first>
  <action>
Edit `apps/figma-plugin/package.json` devDependencies to set these exact version ranges:

```json
"devDependencies": {
  "@figma/plugin-typings": "^1.124.0",
  "@types/node": "^24.0.0",
  "@vitejs/plugin-react": "^6.0.0",
  "postcss-url": "^10.1.3",
  "sass": "^1.60.0",
  "typescript": "^6.0.0",
  "vite": "^8.0.0",
  "vite-plugin-generate-file": "^0.2.0",
  "vite-plugin-react-rich-svg": "^1.0.0",
  "vite-plugin-singlefile": "^2.0.3"
}
```

Changes from current:
- `@figma/plugin-typings`: `^1.83.0` -> `^1.124.0`
- `@types/node`: `^20.0.0` -> `^24.0.0`
- `@vitejs/plugin-react`: `^4.0.0` -> `^6.0.0`
- `typescript`: `^5.3.0` -> `^6.0.0`
- `vite`: `^6.0.0` -> `^8.0.0`

All other devDependencies remain unchanged.

Then run `bun install` from the repo root. This will update `bun.lock`.

After install, verify that `apps/storybook/node_modules/vite` still resolves to Vite 6.x (NOT 8.x) by checking `apps/storybook/node_modules/vite/package.json` version field. Storybook 8.6 does not support Vite 8 — if Bun hoisted Vite 8 into storybook, that is a problem. The storybook package.json already pins `"vite": "^6.0.0"` which should prevent this.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && node -e "const p = require('./apps/figma-plugin/node_modules/vite/package.json'); console.log('vite:', p.version); const t = require('./apps/figma-plugin/node_modules/typescript/package.json'); console.log('ts:', t.version); const f = require('./apps/figma-plugin/node_modules/@figma/plugin-typings/package.json'); console.log('figma:', f.version); const s = require('./apps/storybook/node_modules/vite/package.json'); console.log('storybook-vite:', s.version)" 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
- `apps/figma-plugin/package.json` contains `"vite": "^8.0.0"`
- `apps/figma-plugin/package.json` contains `"typescript": "^6.0.0"`
- `apps/figma-plugin/package.json` contains `"@figma/plugin-typings": "^1.124.0"`
- `apps/figma-plugin/package.json` contains `"@vitejs/plugin-react": "^6.0.0"`
- `apps/figma-plugin/package.json` contains `"@types/node": "^24.0.0"`
- Installed vite version in apps/figma-plugin/node_modules starts with `8.`
- Installed typescript version in apps/figma-plugin/node_modules starts with `6.`
- Installed vite version in apps/storybook/node_modules starts with `6.` (NOT 8)
- `bun install` exits 0
  </acceptance_criteria>
  <done>All five dependency version bumps are in package.json, bun install succeeds, correct versions resolve in node_modules, storybook Vite is still 6.x</done>
</task>

<task type="auto">
  <name>Task 2: Update Vite configs to use rolldownOptions</name>
  <files>apps/figma-plugin/vite.config.plugin.ts, apps/figma-plugin/vite.config.ui.ts</files>
  <read_first>apps/figma-plugin/vite.config.plugin.ts, apps/figma-plugin/vite.config.ui.ts</read_first>
  <action>
In `apps/figma-plugin/vite.config.plugin.ts`, rename `rollupOptions` to `rolldownOptions` on line 22. The entire build block becomes:

```typescript
build: {
  minify: mode === "production",
  sourcemap: mode !== "production" ? "inline" : false,
  target: "es2017",
  emptyOutDir: false,
  outDir: path.resolve(__dirname, "dist"),
  rolldownOptions: {
    input: path.resolve(__dirname, "src/plugin/plugin.ts"),
    output: {
      entryFileNames: "plugin.js",
    },
  },
},
```

In `apps/figma-plugin/vite.config.ui.ts`, rename `rollupOptions` to `rolldownOptions` on line 19. The build block becomes:

```typescript
build: {
  minify: mode === "production",
  cssMinify: mode === "production",
  sourcemap: mode !== "production" ? "inline" : false,
  emptyOutDir: false,
  outDir: path.resolve(__dirname, "dist"),
  rolldownOptions: {
    input: path.resolve(uiSrcPath, "index.html"),
  },
},
```

No other changes to these files. All imports, plugins, css config, and resolve config remain as-is.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && grep -c "rolldownOptions" apps/figma-plugin/vite.config.plugin.ts apps/figma-plugin/vite.config.ui.ts && grep -c "rollupOptions" apps/figma-plugin/vite.config.plugin.ts apps/figma-plugin/vite.config.ui.ts</automated>
  </verify>
  <acceptance_criteria>
- `vite.config.plugin.ts` contains `rolldownOptions` (exactly 1 occurrence)
- `vite.config.plugin.ts` does NOT contain `rollupOptions`
- `vite.config.ui.ts` contains `rolldownOptions` (exactly 1 occurrence)
- `vite.config.ui.ts` does NOT contain `rollupOptions`
- No other lines in either file are changed (imports, plugins, resolve, css config all unchanged)
  </acceptance_criteria>
  <done>Both Vite configs use rolldownOptions instead of rollupOptions, eliminating Vite 8 deprecation warnings</done>
</task>

<task type="auto">
  <name>Task 3: Update tsconfigs for TypeScript 6 compatibility</name>
  <files>apps/figma-plugin/tsconfig.json, apps/figma-plugin/tsconfig.node.json</files>
  <read_first>apps/figma-plugin/tsconfig.json, apps/figma-plugin/tsconfig.node.json</read_first>
  <action>
In `apps/figma-plugin/tsconfig.json`, add `"types": ["node"]` to compilerOptions. The full file becomes:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "Bundler",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "types": ["node"],
    "typeRoots": ["./node_modules/@figma", "../../node_modules/@types"],
    "paths": {
      "monorepo-networker": ["./node_modules/monorepo-networker/dist/index.d.ts"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"]
}
```

In `apps/figma-plugin/tsconfig.node.json`, add `"types": ["node"]` AND change `"moduleResolution": "Node"` to `"moduleResolution": "bundler"`. The full file becomes:

```json
{
  "compilerOptions": {
    "noEmit": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "types": ["node"]
  },
  "include": ["*.ts", "vite.config.*.ts"]
}
```

Key changes:
- `tsconfig.json`: Added `"types": ["node"]` (prevents TS6 default of empty types array dropping Node.js globals). `typeRoots` is preserved for @figma/plugin-typings.
- `tsconfig.node.json`: Added `"types": ["node"]`, changed `"moduleResolution": "Node"` to `"moduleResolution": "bundler"` (Node is deprecated in TS 6).
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && grep '"types"' apps/figma-plugin/tsconfig.json apps/figma-plugin/tsconfig.node.json && grep '"moduleResolution"' apps/figma-plugin/tsconfig.node.json && bun run --filter @repo/figma-plugin types 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `tsconfig.json` contains `"types": ["node"]`
- `tsconfig.json` still contains `"typeRoots": ["./node_modules/@figma", "../../node_modules/@types"]`
- `tsconfig.node.json` contains `"types": ["node"]`
- `tsconfig.node.json` contains `"moduleResolution": "bundler"` (NOT `"Node"`)
- `bun run --filter @repo/figma-plugin types` exits 0 with no errors
  </acceptance_criteria>
  <done>Both tsconfigs are TypeScript 6 compatible with explicit types arrays and no deprecated moduleResolution, and type-checking passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local | Package downloads from public npm registry |
| bun.lock -> node_modules | Lockfile pins exact versions for reproducible installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | npm packages | mitigate | bun.lock committed to git pins exact package hashes; verify install succeeds without integrity warnings |
| T-10-02 | Tampering | vite-plugin-react-rich-svg (unmaintained peerDep) | accept | Plugin uses standard Vite APIs, no elevated privileges; peerDep mismatch is non-fatal warning only; fallback to vite-plugin-svgr exists if needed |
| T-10-03 | Denial of Service | Storybook Vite version conflict | mitigate | Verify apps/storybook/node_modules/vite resolves to 6.x after install; Bun workspace isolation prevents hoisting |
</threat_model>

<verification>
After all tasks complete:
1. `bun run build` completes without errors (BUILD-01)
2. `bun run --filter @repo/figma-plugin types` passes (BUILD-02, FIG-01)
3. `bun run test` passes (no regressions)
4. `bun run build 2>&1 | grep -i deprecat` returns no deprecation warnings
5. `dist/plugin.js` and `dist/index.html` exist after build
6. `dist/manifest.json` exists after build (vite-plugin-generate-file still works)
</verification>

<success_criteria>
- Vite 8.x, TypeScript 6.x, @vitejs/plugin-react 6.x, @figma/plugin-typings 1.124+, and @types/node 24+ are installed
- Both Vite configs use rolldownOptions with zero deprecation warnings
- Both tsconfigs pass type-checking under TypeScript 6
- Build produces dist/plugin.js, dist/index.html, and dist/manifest.json
- All existing tests pass
- Storybook's Vite remains at 6.x (unaffected)
</success_criteria>

<output>
After completion, create `.planning/phases/10-vite-8-typescript-6-figma-typings/10-01-SUMMARY.md`
</output>
