---
phase: 06-react-figma-ui-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/ui/src/app.tsx
  - packages/ui/src/app.module.scss
  - packages/ui/src/__tests__/App.test.tsx
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "The demo app renders all 14 react-figma-ui components in a scrollable panel"
    - "Components are grouped by category: Inputs, Buttons, Display, Layout"
    - "All components use static props with no useState or wired-up logic"
    - "The app builds and type-checks without errors"
  artifacts:
    - path: "packages/ui/src/app.tsx"
      provides: "Static component sampler using all 14 react-figma-ui components"
      contains: "SectionTitle"
    - path: "packages/ui/src/app.module.scss"
      provides: "Scoped layout styles for the sampler container and groups"
      contains: ".container"
    - path: "packages/ui/src/__tests__/App.test.tsx"
      provides: "Smoke render test for the demo app"
      contains: "render(<App"
  key_links:
    - from: "packages/ui/src/app.tsx"
      to: "packages/ui/src/index.ts"
      via: "named imports from @repo/ui barrel"
      pattern: "from '@repo/ui'"
    - from: "packages/ui/src/app.tsx"
      to: "packages/ui/src/app.module.scss"
      via: "CSS Module import"
      pattern: "import styles from './app.module.scss'"
---

<objective>
Rewrite app.tsx as a static component sampler showcasing all 14 react-figma-ui components grouped by category, create scoped layout styles, and add a smoke render test.

Purpose: Provides a working demo that proves all components render correctly in a Figma plugin panel context.
Output: Complete demo app ready for visual verification in Figma.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-react-figma-ui-integration/06-CONTEXT.md
@.planning/phases/06-react-figma-ui-integration/06-RESEARCH.md
@.planning/phases/06-react-figma-ui-integration/06-UI-SPEC.md
@.planning/phases/06-react-figma-ui-integration/06-01-SUMMARY.md

@packages/ui/src/main.tsx
@packages/ui/src/app.network.tsx

<interfaces>
<!-- From Plan 01's rewritten index.ts barrel: -->
From packages/ui/src/index.ts (created in Plan 01):
```typescript
import 'figma-plugin-ds/dist/figma-plugin-ds.css';
export { Button } from 'react-figma-ui';
export { Checkbox } from 'react-figma-ui';
export { Disclosure } from 'react-figma-ui';
export { Icon } from 'react-figma-ui';
export { IconButton } from 'react-figma-ui';
export { Input } from 'react-figma-ui';
export { Label } from 'react-figma-ui';
export { SectionTitle } from 'react-figma-ui';
export { Radio } from 'react-figma-ui';
export { Switch } from 'react-figma-ui';
export { Textarea } from 'react-figma-ui';
export { Type } from 'react-figma-ui';
export { DisclosureTip as DisclosureItem } from 'react-figma-ui';
export { Onboarding as OnboardingTip } from 'react-figma-ui';
export { SelectMenu as Select, SelectMenuOption } from 'react-figma-ui';
export { classes } from './utils/classes.util';
```

<!-- Verified component prop signatures from 06-RESEARCH.md: -->
```typescript
// Button: children, tint?: "primary" | "secondary"
// Checkbox: id, defaultChecked?, children (label)
// Radio: id, name, children (label)
// Switch: id, defaultChecked?, children (label)
// Input: placeholder
// Textarea: placeholder, rows
// Icon: iconName: IconName
// IconButton: iconProps: { iconName }, aria-label
// Label: children
// Type: size?: "small"|"large", weight?: "bold", children
// SectionTitle: children
// OnboardingTip: iconProps: { iconName }, children
// Select: options: T[], render: (item: T, index: number, all: T[]) => ReactElement
// SelectMenuOption: value, children
// Disclosure: tips: T[], render: (tip: T, index: number, all: T[]) => ReactElement
// DisclosureItem: heading, content, expanded?
```

From packages/ui/src/main.tsx (DO NOT MODIFY):
```typescript
// Dynamically imports ./app and renders <App />
// Calls UI_CHANNEL.emit(PLUGIN, "hello", ["Hey there, Figma!"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app.module.scss and rewrite app.tsx as static component sampler</name>
  <files>packages/ui/src/app.tsx, packages/ui/src/app.module.scss</files>
  <read_first>
    - packages/ui/src/app.tsx (current version — will be fully replaced)
    - packages/ui/src/main.tsx (DO NOT modify — understand how App is imported)
    - packages/ui/src/app.network.tsx (DO NOT modify — understand UI_CHANNEL)
    - packages/ui/src/index.ts (Plan 01 output — confirms available exports)
    - .planning/phases/06-react-figma-ui-integration/06-UI-SPEC.md (layout contract, component inventory, copywriting contract)
    - .planning/phases/06-react-figma-ui-integration/06-RESEARCH.md (sections: "Verified Component Prop Signatures", "Pattern 5: SelectMenu Usage", "Pattern 6: Disclosure/DisclosureItem Usage")
  </read_first>
  <action>
1. Create `packages/ui/src/app.module.scss`:
   ```scss
   .container {
     overflow-y: auto;
     height: 100%;
     padding: 8px 16px;
   }

   .group {
     display: flex;
     flex-direction: column;
     gap: 8px;
     margin-bottom: 24px;
   }
   ```

2. Rewrite `packages/ui/src/app.tsx` — complete replacement. The new file must:
   - Default-export a function component named `App`
   - Import CSS Module: `import styles from './app.module.scss';`
   - Import all 14 components + SelectMenuOption from the barrel (`./index` or `@repo/ui` — use relative `./index` since this is within the same package)
   - NOT import UI_CHANNEL, monorepo-networker, or any assets (figma.png, react.svg, vite.svg)
   - NOT use useState or useEffect — purely static
   - Render a scrollable container (`styles.container`) with four category groups (`styles.group`):

   **Inputs group** (SectionTitle "Inputs"):
   - `<Input placeholder="Type something" />`
   - `<Textarea placeholder="Longer text..." rows={3} />`
   - `<Checkbox id="cb1" defaultChecked>Option A</Checkbox>`
   - Two Radio buttons in a group: `<Radio id="r1" name="demo-radio">Option A</Radio>` and `<Radio id="r2" name="demo-radio">Option B</Radio>`
   - `<Switch id="sw1" defaultChecked>Enable feature</Switch>`
   - `<Select options={[{ value: 'a', label: 'Option A' }, { value: 'b', label: 'Option B' }]} render={(opt) => <SelectMenuOption key={opt.value} value={opt.value}>{opt.label}</SelectMenuOption>} />`

   **Buttons group** (SectionTitle "Buttons"):
   - `<Button tint="primary">Primary action</Button>`
   - `<Button>Secondary action</Button>`
   - `<IconButton iconProps={{ iconName: 'plus' }} aria-label="Add item" />`

   **Display group** (SectionTitle "Display"):
   - `<Icon iconName="star" />`
   - `<Label>Field label</Label>`
   - `<Type size="large" weight="bold">Heading text</Type>`
   - `<OnboardingTip iconProps={{ iconName: 'info' }}>This is a tip for new users.</OnboardingTip>`

   **Layout group** (SectionTitle "Layout"):
   - ```tsx
     <Disclosure
       tips={[{ heading: 'More info', content: 'Details here.' }]}
       render={(tip) => (
         <DisclosureItem
           key={tip.heading}
           heading={tip.heading}
           content={tip.content}
           expanded
         />
       )}
     />
     ```

   The exact copy text comes from the UI-SPEC Copywriting Contract. Use those exact strings.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && grep -q "import styles from './app.module.scss'" packages/ui/src/app.tsx && grep -q "SectionTitle" packages/ui/src/app.tsx && grep -c "SectionTitle" packages/ui/src/app.tsx | grep -qE "^[4-9]" && ! grep -q "useState" packages/ui/src/app.tsx && ! grep -q "UI_CHANNEL" packages/ui/src/app.tsx && test -f packages/ui/src/app.module.scss && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/app.tsx default-exports an `App` component
    - packages/ui/src/app.tsx imports styles from `./app.module.scss`
    - packages/ui/src/app.tsx contains 4 SectionTitle instances with text: "Inputs", "Buttons", "Display", "Layout"
    - packages/ui/src/app.tsx does NOT contain `useState`, `useEffect`, `UI_CHANNEL`, or asset imports
    - packages/ui/src/app.tsx renders all 14 components: Button, Checkbox, Disclosure, DisclosureItem, Icon, IconButton, Input, Label, SectionTitle, OnboardingTip, Radio, Select, SelectMenuOption, Switch, Textarea, Type
    - packages/ui/src/app.module.scss contains `.container` with `overflow-y: auto` and `.group` with `gap: 8px`
  </acceptance_criteria>
  <done>app.tsx is a static component sampler with all 14 components grouped by category, app.module.scss provides scoped layout, no state or side effects</done>
</task>

<task type="auto">
  <name>Task 2: Add App smoke render test and verify full build + type-check</name>
  <files>packages/ui/src/__tests__/App.test.tsx</files>
  <read_first>
    - packages/ui/src/app.tsx (after Task 1 rewrite)
    - packages/ui/src/__tests__/exports.test.ts (Plan 01 test — confirm pattern)
    - packages/ui/vitest.config.ts
  </read_first>
  <action>
1. Create `packages/ui/src/__tests__/App.test.tsx`:
   ```tsx
   import { render, screen } from '@testing-library/react';
   import { describe, expect, it } from 'vitest';
   import App from '../app';

   describe('App (component sampler)', () => {
     it('renders without crashing', () => {
       render(<App />);
       expect(screen.getByText('Inputs')).toBeInTheDocument();
       expect(screen.getByText('Buttons')).toBeInTheDocument();
       expect(screen.getByText('Display')).toBeInTheDocument();
       expect(screen.getByText('Layout')).toBeInTheDocument();
     });

     it('renders primary button', () => {
       render(<App />);
       expect(screen.getByText('Primary action')).toBeInTheDocument();
     });

     it('renders onboarding tip', () => {
       render(<App />);
       expect(screen.getByText('This is a tip for new users.')).toBeInTheDocument();
     });
   });
   ```

2. Run tests: `cd /Users/jonasbroms/Sites/figma-plugin-template && bun run test --filter @repo/ui`

3. Run type-check: `cd /Users/jonasbroms/Sites/figma-plugin-template && bun run types --filter @repo/figma-plugin`

4. Run full build: `cd /Users/jonasbroms/Sites/figma-plugin-template && bun run build --filter @repo/figma-plugin`

All three commands must pass. If type-check or build fails, diagnose and fix the issue in app.tsx or index.ts before proceeding.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && bun run test --filter @repo/ui && bun run types --filter @repo/figma-plugin && bun run build --filter @repo/figma-plugin && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/__tests__/App.test.tsx exists and contains `render(<App />)`
    - `bun run test --filter @repo/ui` passes (exports.test.ts, classes.util.test.ts, App.test.tsx all green)
    - `bun run types --filter @repo/figma-plugin` exits 0 (no type errors)
    - `bun run build --filter @repo/figma-plugin` exits 0 (dist/ output generated with inlined CSS)
  </acceptance_criteria>
  <done>All tests pass, type-check clean, production build succeeds with react-figma-ui components and figma-plugin-ds CSS inlined</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only modifies internal app code using dependencies installed in Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Spoofing | Static demo app | accept | Demo app is a static sampler with no user input processing, no auth, no data persistence |
</threat_model>

<verification>
1. `bun run test --filter @repo/ui` -- all 3 test files pass
2. `bun run types --filter @repo/figma-plugin` -- zero type errors
3. `bun run build --filter @repo/figma-plugin` -- dist/ produced successfully
4. `grep -c "SectionTitle" packages/ui/src/app.tsx` returns 4+ (4 section headings + import)
</verification>

<success_criteria>
- app.tsx is a static component sampler with all 14 react-figma-ui components
- app.module.scss provides scoped layout for the sampler
- App.test.tsx smoke-renders the sampler and asserts section headings present
- Full test suite, type-check, and build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-react-figma-ui-integration/06-02-SUMMARY.md`
</output>
