---
phase: 14-storybook-10-upgrade
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - apps/storybook/src/stories/Button.stories.tsx
  - apps/storybook/src/stories/Checkbox.stories.tsx
  - apps/storybook/src/stories/Input.stories.tsx
  - apps/storybook/src/stories/Select.stories.tsx
  - apps/storybook/src/stories/Disclosure.stories.tsx
  - apps/storybook/src/stories/DisclosureItem.stories.tsx
  - apps/storybook/src/stories/Radio.stories.tsx
  - apps/storybook/src/stories/OnboardingTip.stories.tsx
  - apps/storybook/src/stories/Switch.stories.tsx
  - apps/storybook/src/stories/Icon.stories.tsx
  - apps/storybook/src/stories/IconButton.stories.tsx
  - apps/storybook/src/stories/Label.stories.tsx
  - apps/storybook/src/stories/SectionTitle.stories.tsx
  - apps/storybook/src/stories/Textarea.stories.tsx
  - apps/storybook/src/stories/Type.stories.tsx
  - apps/storybook/src/stories/Accordion.stories.tsx
  - apps/storybook/src/stories/Alert.stories.tsx
  - apps/storybook/src/stories/RadioGroup.stories.tsx
autonomous: true
requirements: [FW-02]

must_haves:
  truths:
    - "All stories compile without TypeScript errors"
    - "build-storybook completes successfully"
    - "Each shadcn/ui component has at least one story with Autodocs"
    - "Story files use correct current component APIs from @repo/ui"
    - "Old react-figma-ui story patterns (tint, destructive, iconProps on Input, SelectMenuOption, DisclosureItem) are removed"
  artifacts:
    - path: "apps/storybook/src/stories/Accordion.stories.tsx"
      provides: "Accordion story replacing Disclosure"
      contains: "AccordionTrigger"
    - path: "apps/storybook/src/stories/Alert.stories.tsx"
      provides: "Alert story replacing OnboardingTip"
      contains: "AlertTitle"
    - path: "apps/storybook/src/stories/RadioGroup.stories.tsx"
      provides: "RadioGroup story replacing Radio"
      contains: "RadioGroupItem"
    - path: "apps/storybook/src/stories/Button.stories.tsx"
      provides: "Updated Button story with variant prop"
      contains: "variant"
    - path: "apps/storybook/src/stories/Select.stories.tsx"
      provides: "Rewritten Select story with composition API"
      contains: "SelectTrigger"
  key_links:
    - from: "apps/storybook/src/stories/*.stories.tsx"
      to: "@repo/ui"
      via: "import { Component } from '@repo/ui'"
      pattern: "from '@repo/ui'"
---

<objective>
Rewrite all 15 existing stories for the new shadcn/ui component APIs, rename files to match new component names, and delete obsolete story files.

Purpose: Complete the Storybook 10 migration by ensuring every component exported from @repo/ui has a working story with Autodocs and Controls.
Output: 14 story files (some renamed) that compile cleanly and render in Storybook 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-storybook-10-upgrade/14-RESEARCH.md
@.planning/phases/14-storybook-10-upgrade/14-01-SUMMARY.md

@packages/ui/src/index.ts

<interfaces>
<!-- Current @repo/ui exports that stories must use -->

From packages/ui/src/index.ts:
```typescript
export { Button, type ButtonProps, buttonVariants } from './components/ui/button';
// Button: variant='default'|'outline'|'secondary'|'ghost'|'destructive'|'link', size='default'|'xs'|'sm'|'lg'|'icon'|'icon-xs'|'icon-sm'|'icon-lg'

export { Checkbox } from './components/ui/checkbox';
// Checkbox: Radix CheckboxPrimitive.Root — no children/label prop; compose with Label

export { Input } from './components/ui/input';
// Input: standard HTML input props — NO iconProps

export { Label } from './components/ui/label';
// Label: standard HTML label wrapper

export { Select, SelectContent, SelectGroup, SelectItem, SelectLabel, SelectScrollDownButton, SelectScrollUpButton, SelectSeparator, SelectTrigger, SelectValue } from './components/ui/select';
// Select: composition pattern — Select > SelectTrigger > SelectValue + SelectContent > SelectItem

export { Switch } from './components/ui/switch';
// Switch: Radix SwitchPrimitive.Root — no children/label prop; compose with Label

export { Textarea } from './components/ui/textarea';
// Textarea: standard HTML textarea props

export { RadioGroup, RadioGroupItem } from './components/ui/radio-group';
// RadioGroup: Radix — RadioGroup > div > RadioGroupItem + Label

export { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from './components/ui/accordion';
// Accordion: type="single"|"multiple", collapsible; AccordionItem value="..."> AccordionTrigger + AccordionContent

export { Alert, AlertDescription, AlertTitle } from './components/ui/alert';
// Alert: variant='default'|'destructive'; compose AlertTitle + AlertDescription inside

export { Icon, type IconProps } from './components/figma/icon';
// Icon: iconName='plus'|'info'|'star' ONLY (others render null); spin?: boolean

export { IconButton } from './components/figma/icon-button';
// IconButton: iconProps: IconProps, selected?: boolean, variant/size from Button

export { SectionTitle } from './components/figma/section-title';
// SectionTitle: children: ReactNode

export { Type } from './components/figma/type';
// Type: size='xsmall'|'small'|'large'|'xlarge', weight='normal'|'medium'|'bold' — NO inverse prop
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite stories with breaking API changes</name>
  <files>
    apps/storybook/src/stories/Button.stories.tsx,
    apps/storybook/src/stories/Select.stories.tsx,
    apps/storybook/src/stories/Disclosure.stories.tsx,
    apps/storybook/src/stories/DisclosureItem.stories.tsx,
    apps/storybook/src/stories/OnboardingTip.stories.tsx,
    apps/storybook/src/stories/Radio.stories.tsx,
    apps/storybook/src/stories/Input.stories.tsx,
    apps/storybook/src/stories/Accordion.stories.tsx,
    apps/storybook/src/stories/Alert.stories.tsx,
    apps/storybook/src/stories/RadioGroup.stories.tsx
  </files>
  <read_first>
    apps/storybook/src/stories/Button.stories.tsx,
    apps/storybook/src/stories/Select.stories.tsx,
    apps/storybook/src/stories/Disclosure.stories.tsx,
    apps/storybook/src/stories/DisclosureItem.stories.tsx,
    apps/storybook/src/stories/OnboardingTip.stories.tsx,
    apps/storybook/src/stories/Radio.stories.tsx,
    apps/storybook/src/stories/Input.stories.tsx,
    packages/ui/src/components/ui/button.tsx,
    packages/ui/src/components/ui/select.tsx,
    packages/ui/src/components/ui/accordion.tsx,
    packages/ui/src/components/ui/alert.tsx,
    packages/ui/src/components/ui/radio-group.tsx,
    packages/ui/src/components/ui/checkbox.tsx,
    packages/ui/src/components/figma/icon.tsx
  </read_first>
  <action>
Rewrite 7 story files with breaking changes and create 3 renamed replacements. Use CSF3 format with `satisfies Meta<typeof Component>` and `StoryObj<typeof meta>`. All types from `@storybook/react`.

**1. Button.stories.tsx** — Replace `tint`/`destructive` props with `variant`:
- Default: `{ children: 'Click me' }` (no variant = default)
- Primary: REMOVE (was `tint: 'primary'`; shadcn default IS the primary). Replace with Outline: `{ children: 'Outline', variant: 'outline' }`
- Secondary: `{ children: 'Secondary', variant: 'secondary' }`
- Destructive: `{ children: 'Delete', variant: 'destructive' }`
- Add Ghost: `{ children: 'Ghost', variant: 'ghost' }`
- Add Small: `{ children: 'Small', size: 'sm' }`

**2. Input.stories.tsx** — Remove WithIcon story (shadcn Input has no iconProps):
- Default: keep as-is
- WithValue: keep as-is
- WithIcon: DELETE this story entirely
- Disabled: keep as-is

**3. Select.stories.tsx** — Full rewrite using composition pattern:
- Import `{ Select, SelectTrigger, SelectValue, SelectContent, SelectItem }` from `@repo/ui`
- meta component: `Select` (keep title 'Components/Select')
- Default story with `render`:
  ```tsx
  render: () => (
    <Select>
      <SelectTrigger className="w-[180px]">
        <SelectValue placeholder="Choose option" />
      </SelectTrigger>
      <SelectContent>
        <SelectItem value="option1">Option 1</SelectItem>
        <SelectItem value="option2">Option 2</SelectItem>
        <SelectItem value="option3">Option 3</SelectItem>
      </SelectContent>
    </Select>
  )
  ```
- ManyOptions story: same pattern with Left/Center/Right/Justify items

**4. Disclosure.stories.tsx -> DELETE this file.** Create new `Accordion.stories.tsx`:
- Import `{ Accordion, AccordionItem, AccordionTrigger, AccordionContent }` from `@repo/ui`
- meta component: `Accordion`, title: `'Components/Accordion'`
- Default story with render:
  ```tsx
  render: () => (
    <Accordion type="single" collapsible className="w-full">
      <AccordionItem value="item-1">
        <AccordionTrigger>Section 1</AccordionTrigger>
        <AccordionContent>Content for section 1</AccordionContent>
      </AccordionItem>
      <AccordionItem value="item-2">
        <AccordionTrigger>Section 2</AccordionTrigger>
        <AccordionContent>Content for section 2</AccordionContent>
      </AccordionItem>
      <AccordionItem value="item-3">
        <AccordionTrigger>Section 3</AccordionTrigger>
        <AccordionContent>Content for section 3</AccordionContent>
      </AccordionItem>
    </Accordion>
  )
  ```
- Multiple story: `type="multiple"` with defaultValue={['item-1']}

**5. DisclosureItem.stories.tsx -> DELETE this file.** AccordionItem is only meaningful inside Accordion; no standalone story.

**6. OnboardingTip.stories.tsx -> DELETE this file.** Create new `Alert.stories.tsx`:
- Import `{ Alert, AlertTitle, AlertDescription }` from `@repo/ui`
- meta component: `Alert`, title: `'Components/Alert'`
- Default story with render:
  ```tsx
  render: () => (
    <Alert>
      <AlertTitle>Tip</AlertTitle>
      <AlertDescription>Select a layer to get started.</AlertDescription>
    </Alert>
  )
  ```
- Destructive story: `variant: 'destructive'` with warning text

**7. Radio.stories.tsx -> DELETE this file.** Create new `RadioGroup.stories.tsx`:
- Import `{ RadioGroup, RadioGroupItem }` and `{ Label }` from `@repo/ui`
- meta component: `RadioGroup`, title: `'Components/RadioGroup'`
- Default story with render:
  ```tsx
  render: () => (
    <RadioGroup defaultValue="option-a">
      <div className="flex items-center gap-2">
        <RadioGroupItem value="option-a" id="option-a" />
        <Label htmlFor="option-a">Option A</Label>
      </div>
      <div className="flex items-center gap-2">
        <RadioGroupItem value="option-b" id="option-b" />
        <Label htmlFor="option-b">Option B</Label>
      </div>
      <div className="flex items-center gap-2">
        <RadioGroupItem value="option-c" id="option-c" />
        <Label htmlFor="option-c">Option C</Label>
      </div>
    </RadioGroup>
  )
  ```
- WithDefault story: `defaultValue="option-b"` to show pre-selected state

After creating all files, delete the old files:
```bash
rm apps/storybook/src/stories/Disclosure.stories.tsx
rm apps/storybook/src/stories/DisclosureItem.stories.tsx
rm apps/storybook/src/stories/OnboardingTip.stories.tsx
rm apps/storybook/src/stories/Radio.stories.tsx
```
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && test -f apps/storybook/src/stories/Accordion.stories.tsx && test -f apps/storybook/src/stories/Alert.stories.tsx && test -f apps/storybook/src/stories/RadioGroup.stories.tsx && ! test -f apps/storybook/src/stories/Disclosure.stories.tsx && ! test -f apps/storybook/src/stories/DisclosureItem.stories.tsx && ! test -f apps/storybook/src/stories/OnboardingTip.stories.tsx && ! test -f apps/storybook/src/stories/Radio.stories.tsx && ! grep -q "tint" apps/storybook/src/stories/Button.stories.tsx && ! grep -q "iconProps" apps/storybook/src/stories/Input.stories.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Accordion.stories.tsx exists and contains "AccordionTrigger"
    - Alert.stories.tsx exists and contains "AlertTitle"
    - RadioGroup.stories.tsx exists and contains "RadioGroupItem"
    - Disclosure.stories.tsx does NOT exist
    - DisclosureItem.stories.tsx does NOT exist
    - OnboardingTip.stories.tsx does NOT exist
    - Radio.stories.tsx does NOT exist
    - Button.stories.tsx contains "variant" and does NOT contain "tint" or "destructive: true"
    - Input.stories.tsx does NOT contain "iconProps"
    - Select.stories.tsx contains "SelectTrigger" and does NOT contain "SelectMenuOption"
  </acceptance_criteria>
  <done>7 breaking stories rewritten; 3 renamed stories created; 4 obsolete files deleted</done>
</task>

<task type="auto">
  <name>Task 2: Update minor stories and verify full build</name>
  <files>
    apps/storybook/src/stories/Checkbox.stories.tsx,
    apps/storybook/src/stories/Switch.stories.tsx,
    apps/storybook/src/stories/Icon.stories.tsx,
    apps/storybook/src/stories/IconButton.stories.tsx,
    apps/storybook/src/stories/Label.stories.tsx,
    apps/storybook/src/stories/SectionTitle.stories.tsx,
    apps/storybook/src/stories/Textarea.stories.tsx,
    apps/storybook/src/stories/Type.stories.tsx
  </files>
  <read_first>
    apps/storybook/src/stories/Checkbox.stories.tsx,
    apps/storybook/src/stories/Switch.stories.tsx,
    apps/storybook/src/stories/Icon.stories.tsx,
    apps/storybook/src/stories/Type.stories.tsx,
    apps/storybook/src/stories/IconButton.stories.tsx,
    apps/storybook/src/stories/Label.stories.tsx,
    apps/storybook/src/stories/SectionTitle.stories.tsx,
    apps/storybook/src/stories/Textarea.stories.tsx,
    packages/ui/src/components/figma/type.tsx,
    packages/ui/src/components/figma/icon.tsx,
    packages/ui/src/components/figma/icon-button.tsx
  </read_first>
  <action>
Update 8 story files with minor fixes:

**1. Checkbox.stories.tsx** — shadcn Checkbox has NO `children` prop. Rewrite to use composition with Label:
- Import `{ Checkbox }` and `{ Label }` from `@repo/ui`
- Default story with render:
  ```tsx
  render: () => (
    <div className="flex items-center gap-2">
      <Checkbox id="checkbox-default" />
      <Label htmlFor="checkbox-default">Enable option</Label>
    </div>
  )
  ```
- Checked: same pattern with `defaultChecked` on Checkbox
- Disabled: same pattern with `disabled` on Checkbox

**2. Switch.stories.tsx** — shadcn Switch has NO `children` prop. Rewrite to use composition with Label:
- Import `{ Switch }` and `{ Label }` from `@repo/ui`
- Default story with render:
  ```tsx
  render: () => (
    <div className="flex items-center gap-2">
      <Switch id="switch-default" />
      <Label htmlFor="switch-default">Enable feature</Label>
    </div>
  )
  ```
- On: same with `defaultChecked`
- Disabled: same with `disabled`

**3. Icon.stories.tsx** — Replace invalid icon names. Only `plus`, `info`, `star` exist in iconMap:
- Default: `{ iconName: 'info' }` (was 'search' which renders null)
- Plus: `{ iconName: 'plus' }` (renamed from Alert)
- Star: `{ iconName: 'star' }` (renamed from Colored)
- Spinning: `{ iconName: 'info', spin: true }` (renamed from Spinner; info exists, spinner doesn't)
- Remove `colorName` arg (not a valid prop on Icon — Icon extends LucideProps which uses `color` or `className`)

**4. Type.stories.tsx** — Remove the `Inverse` story (Type component has no `inverse` prop):
- Default: keep
- Small: keep
- Large: keep
- Bold: keep
- Inverse: DELETE this story
- Add Medium: `{ children: 'Medium weight', weight: 'medium' }`
- Add XSmall: `{ children: 'Extra small', size: 'xsmall' }`

**5. IconButton.stories.tsx** — Verify `iconProps` usage matches current API. Read the component first. The current IconButton accepts `iconProps: IconProps` where IconProps has `iconName` and `spin`. Update icon names to valid ones (plus, info, star only). Verify `selected` prop still exists.

**6. Label.stories.tsx** — Likely correct as-is. Read and verify no breaking changes. Label is a standard wrapper.

**7. SectionTitle.stories.tsx** — Custom Figma component, API unchanged. Read and verify.

**8. Textarea.stories.tsx** — Standard HTML textarea props. Read and verify.

After all updates, run the full build to verify everything compiles:
```bash
cd /Users/jonasbroms/Sites/figma-plugin-template && turbo run build-storybook
```

The build MUST complete with exit code 0. If TypeScript errors remain, fix them.

Then run Biome lint on all story files:
```bash
cd /Users/jonasbroms/Sites/figma-plugin-template && bun run lint
```

Fix any lint errors.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && turbo run build-storybook 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - turbo run build-storybook exits with code 0
    - bun run lint passes with no errors in story files
    - grep "inverse" apps/storybook/src/stories/Type.stories.tsx returns 0 matches
    - grep "children" apps/storybook/src/stories/Checkbox.stories.tsx returns 0 matches (uses Label composition)
    - grep "children" apps/storybook/src/stories/Switch.stories.tsx returns 0 matches (uses Label composition)
    - Icon.stories.tsx contains only iconName values "plus", "info", or "star"
    - All 14 story files exist: Accordion, Alert, Button, Checkbox, Icon, IconButton, Input, Label, RadioGroup, SectionTitle, Select, Switch, Textarea, Type
  </acceptance_criteria>
  <done>All 14 story files compile cleanly; build-storybook succeeds; lint passes; every exported component has at least one story with Autodocs enabled via global tags</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Story files -> @repo/ui | Stories import components; no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Information Disclosure | Story files | accept | Stories are dev-only (not shipped in plugin build); no secrets or PII in component examples |
</threat_model>

<verification>
- `turbo run build-storybook` exits 0
- `bun run lint` passes
- 14 story files exist in apps/storybook/src/stories/
- No references to removed APIs: `tint`, `SelectMenuOption`, `DisclosureItem`, `Disclosure` (as component), `OnboardingTip`, `Radio` (as standalone), `inverse` on Type, `iconProps` on Input
- Each story file imports from `@repo/ui` and uses CSF3 format
</verification>

<success_criteria>
All 14 component stories compile, render in Storybook 10, and have Autodocs enabled. The build-storybook command completes without errors. Old react-figma-ui patterns are fully removed.
</success_criteria>

<output>
After completion, create `.planning/phases/14-storybook-10-upgrade/14-02-SUMMARY.md`
</output>
