---
phase: 08-claude-skills-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/settings.local.json
autonomous: true
requirements:
  - SKILL-01
  - SKILL-03

must_haves:
  truths:
    - "No virke-* agent files exist anywhere under .claude/ or .agents/"
    - "settings.local.json includes permissions for all Figma skills and key GSD workflows"
  artifacts:
    - path: ".claude/settings.local.json"
      provides: "Expanded Claude Code permissions"
      contains: "figma-api"
  key_links:
    - from: ".claude/settings.local.json"
      to: ".claude/skills/ symlinks"
      via: "Skill() permission entries"
      pattern: "Skill\\(figma-"
---

<objective>
Audit agent files for virke-* duplicates and expand Claude Code permissions for all project skills.

Purpose: Ensure no duplicate agent configurations exist and that Claude Code has pre-approved permissions for all Figma skills and common GSD workflows, eliminating permission prompts during development.
Output: Clean .claude/ directory and expanded settings.local.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and remove virke-* duplicate agents (SKILL-01)</name>
  <files>.claude/agents/, .agents/</files>
  <read_first>
    - List all contents of .claude/ recursively to find any virke-* files or directories
    - List all contents of .agents/ recursively to find any virke-* files or directories
    - Check for any files containing "virke" references: grep -r "virke" .claude/ .agents/ 2>/dev/null
  </read_first>
  <action>
Search for any virke-* agent files or directories under .claude/ and .agents/ that duplicate gsd-* functionality. Based on current codebase analysis, .claude/agents/ does not exist and .agents/skills/ contains only Figma skills (figma-api, figma-code-connect, figma-create-design-system-rules, figma-create-new-file, figma-generate-design, figma-generate-library, figma-implement-design, figma-use) plus find-skills and skill-creator. None are virke-* duplicates.

If any virke-* files ARE found during execution:
1. Compare their functionality against gsd-* equivalents (list in $HOME/.claude/get-shit-done/workflows/)
2. Delete any that duplicate gsd-* functionality
3. Document what was removed and why

If NO virke-* files are found (expected case):
1. Confirm the audit is clean
2. Document the finding in the summary

Do NOT create or modify any files if nothing needs removal.
  </action>
  <verify>
    <automated>test -z "$(find .claude/ .agents/ -name 'virke-*' 2>/dev/null)" && echo "PASS: No virke-* files found"</automated>
  </verify>
  <acceptance_criteria>No virke-* agent files or directories exist under .claude/ or .agents/. Audit documented in summary.</acceptance_criteria>
  <done>Zero virke-* files exist in .claude/ and .agents/ directories.</done>
</task>

<task type="auto">
  <name>Task 2: Expand settings.local.json with skill permissions (SKILL-03)</name>
  <files>.claude/settings.local.json</files>
  <read_first>
    - .claude/settings.local.json (current permissions)
    - List .claude/skills/ to get all symlinked skill names
    - List .agents/skills/ to get all available skill names
  </read_first>
  <action>
Rewrite .claude/settings.local.json to include permissions for all Figma skills and commonly used GSD workflows. The current file has only 3 entries:

```json
{
  "permissions": {
    "allow": [
      "Skill(gsd-plan-phase)",
      "WebSearch",
      "Skill(gsd-execute-phase)"
    ]
  }
}
```

Replace with expanded permissions including:

**GSD workflows** (add the most commonly used ones):
- Skill(gsd-plan-phase)
- Skill(gsd-execute-phase)
- Skill(gsd-discuss-phase)
- Skill(gsd-research-phase)
- Skill(gsd-verify-phase)
- Skill(gsd-new-project)
- Skill(gsd-new-milestone)
- Skill(gsd-resume-project)
- Skill(gsd-progress)
- Skill(gsd-next)
- Skill(gsd-health)
- Skill(gsd-map-codebase)
- Skill(gsd-code-review)

**Figma skills** (all 8 from .agents/skills/):
- Skill(figma-api)
- Skill(figma-code-connect)
- Skill(figma-create-design-system-rules)
- Skill(figma-create-new-file)
- Skill(figma-generate-design)
- Skill(figma-generate-library)
- Skill(figma-implement-design)
- Skill(figma-use)

**Utility skills**:
- Skill(find-skills)
- Skill(skill-creator)

**Other**:
- WebSearch

Keep the JSON properly formatted with 2-space indentation. Sort permissions alphabetically within the allow array for readability.
  </action>
  <verify>
    <automated>node -e "const s=JSON.parse(require('fs').readFileSync('.claude/settings.local.json','utf8')); const a=s.permissions.allow; console.log('Entries:', a.length); const needed=['figma-api','figma-use','gsd-execute-phase','gsd-plan-phase','gsd-verify-phase']; const missing=needed.filter(n=>!a.some(e=>e.includes(n))); if(missing.length){console.log('FAIL: Missing',missing);process.exit(1)}else{console.log('PASS: All key permissions present')}"</automated>
  </verify>
  <acceptance_criteria>settings.local.json contains permissions for all 8 Figma skills, 13+ GSD workflows, utility skills, and WebSearch. File is valid JSON.</acceptance_criteria>
  <done>settings.local.json has 24+ permission entries covering all Figma skills and key GSD workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan modifies local IDE configuration only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I (Info Disclosure) | settings.local.json | accept | File contains only permission names, no secrets. Already in .gitignore if sensitive. |
</threat_model>

<verification>
- No virke-* files exist under .claude/ or .agents/
- settings.local.json is valid JSON with 24+ permission entries
- All 8 Figma skill names appear in permissions
- Key GSD workflows (plan, execute, verify, discuss, research) appear in permissions
</verification>

<success_criteria>
SKILL-01 satisfied: No duplicate virke-* agents exist (confirmed by audit)
SKILL-03 satisfied: settings.local.json has comprehensive permissions for all project skills
</success_criteria>

<output>
After completion, create `.planning/phases/08-claude-skills-optimization/08-01-SUMMARY.md`
</output>
