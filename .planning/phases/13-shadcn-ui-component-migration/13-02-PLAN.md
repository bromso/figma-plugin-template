---
phase: 13-shadcn-ui-component-migration
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - packages/ui/src/components/ui/button.tsx
  - packages/ui/src/components/ui/checkbox.tsx
  - packages/ui/src/components/ui/input.tsx
  - packages/ui/src/components/ui/label.tsx
  - packages/ui/src/components/ui/select.tsx
  - packages/ui/src/components/ui/switch.tsx
  - packages/ui/src/components/ui/textarea.tsx
  - packages/ui/src/components/ui/radio-group.tsx
  - packages/ui/src/components/ui/accordion.tsx
  - packages/ui/src/components/ui/alert.tsx
  - packages/ui/src/components/figma/icon.tsx
  - packages/ui/src/components/figma/icon-button.tsx
  - packages/ui/src/components/figma/section-title.tsx
  - packages/ui/src/components/figma/type.tsx
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "10 shadcn/ui components exist in packages/ui/src/components/ui/"
    - "4 custom Figma components exist in packages/ui/src/components/figma/"
    - "All 14 component equivalents are importable React components"
  artifacts:
    - path: "packages/ui/src/components/ui/button.tsx"
      provides: "Button with variant/size props"
      exports: ["Button"]
    - path: "packages/ui/src/components/ui/checkbox.tsx"
      provides: "Accessible checkbox via Radix"
      exports: ["Checkbox"]
    - path: "packages/ui/src/components/ui/input.tsx"
      provides: "Styled text input"
      exports: ["Input"]
    - path: "packages/ui/src/components/ui/label.tsx"
      provides: "Form label"
      exports: ["Label"]
    - path: "packages/ui/src/components/ui/select.tsx"
      provides: "Accessible select dropdown"
      exports: ["Select", "SelectContent", "SelectItem", "SelectTrigger", "SelectValue"]
    - path: "packages/ui/src/components/ui/switch.tsx"
      provides: "Toggle switch"
      exports: ["Switch"]
    - path: "packages/ui/src/components/ui/textarea.tsx"
      provides: "Multiline text input"
      exports: ["Textarea"]
    - path: "packages/ui/src/components/ui/radio-group.tsx"
      provides: "Radio group with items"
      exports: ["RadioGroup", "RadioGroupItem"]
    - path: "packages/ui/src/components/ui/accordion.tsx"
      provides: "Collapsible sections"
      exports: ["Accordion", "AccordionItem", "AccordionTrigger", "AccordionContent"]
    - path: "packages/ui/src/components/ui/alert.tsx"
      provides: "Alert/tip callout"
      exports: ["Alert", "AlertDescription", "AlertTitle"]
    - path: "packages/ui/src/components/figma/icon.tsx"
      provides: "Lucide icon wrapper for Figma icon names"
      exports: ["Icon"]
    - path: "packages/ui/src/components/figma/icon-button.tsx"
      provides: "Button size=icon with Icon child"
      exports: ["IconButton"]
    - path: "packages/ui/src/components/figma/section-title.tsx"
      provides: "Panel section heading"
      exports: ["SectionTitle"]
    - path: "packages/ui/src/components/figma/type.tsx"
      provides: "Typography text component"
      exports: ["Type"]
  key_links:
    - from: "packages/ui/src/components/ui/button.tsx"
      to: "@/lib/utils"
      via: "import cn"
      pattern: "from \"@/lib/utils\""
    - from: "packages/ui/src/components/figma/icon-button.tsx"
      to: "packages/ui/src/components/ui/button.tsx"
      via: "import Button"
      pattern: "from \"@/components/ui/button\""
---

<objective>
Add all 14 component equivalents: 10 via shadcn CLI (Button, Checkbox, Input, Label, Select, Switch, Textarea, RadioGroup, Accordion, Alert) and 4 custom Figma-specific components (Icon, IconButton, SectionTitle, Type).

Purpose: Creates the full component set that replaces react-figma-ui, ready to be wired into exports and the demo app.
Output: 14 component files in packages/ui/src/components/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-shadcn-ui-component-migration/13-RESEARCH.md
@.planning/phases/13-shadcn-ui-component-migration/13-01-SUMMARY.md

<interfaces>
<!-- cn() utility created in Plan 01 -->
```typescript
// packages/ui/src/lib/utils.ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<!-- components.json created in Plan 01 -->
```json
{
  "style": "radix-nova",
  "rsc": false,
  "aliases": {
    "components": "@repo/ui/components",
    "utils": "@repo/ui/lib/utils",
    "ui": "@repo/ui/components/ui"
  }
}
```

<!-- react-figma-ui component API mapping (from RESEARCH.md) -->
<!-- Icon: iconName (string), spin? (boolean) -->
<!-- IconButton: iconProps ({ iconName }), selected? (boolean) -->
<!-- SectionTitle: children (ReactNode) -->
<!-- Type: size ('xsmall'|'small'|'large'|'xlarge'), weight ('normal'|'medium'|'bold'), children -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 10 shadcn/ui components via CLI</name>
  <files>
    packages/ui/src/components/ui/button.tsx,
    packages/ui/src/components/ui/checkbox.tsx,
    packages/ui/src/components/ui/input.tsx,
    packages/ui/src/components/ui/label.tsx,
    packages/ui/src/components/ui/select.tsx,
    packages/ui/src/components/ui/switch.tsx,
    packages/ui/src/components/ui/textarea.tsx,
    packages/ui/src/components/ui/radio-group.tsx,
    packages/ui/src/components/ui/accordion.tsx,
    packages/ui/src/components/ui/alert.tsx
  </files>
  <read_first>
    - packages/ui/components.json
    - packages/ui/src/lib/utils.ts
    - packages/ui/package.json
  </read_first>
  <action>
1. Change to the packages/ui directory (where components.json lives) and run the shadcn CLI to add all 10 components in one command:
   ```bash
   cd packages/ui && bunx shadcn@latest add button checkbox input label select switch textarea radio-group accordion alert --yes --overwrite
   ```
   The `--yes` flag skips confirmation prompts. The CLI reads components.json for output paths and aliases.

2. After the CLI completes, verify all 10 component files exist in `packages/ui/src/components/ui/`:
   - button.tsx, checkbox.tsx, input.tsx, label.tsx, select.tsx, switch.tsx, textarea.tsx, radio-group.tsx, accordion.tsx, alert.tsx

3. If the CLI did NOT auto-install peer deps (check if `radix-ui` is in package.json dependencies), ensure it is present. It should already be installed from Plan 01. Run `bun install` from repo root to sync.

4. Verify the generated files use `@/lib/utils` imports (which resolve via the @/ alias configured in Plan 01).

IMPORTANT: If the shadcn CLI fails or places files in unexpected locations, manually create the component files using the standard shadcn/ui source code. Each component imports `cn` from `@/lib/utils` and uses Radix primitives from `radix-ui`. The shadcn CLI is a convenience — the components are just source files.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && for f in button checkbox input label select switch textarea radio-group accordion alert; do test -f "packages/ui/src/components/ui/$f.tsx" || (echo "MISSING: $f.tsx" && exit 1); done && echo "All 10 shadcn components present"</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/components/ui/button.tsx exists and exports Button
    - packages/ui/src/components/ui/checkbox.tsx exists and exports Checkbox
    - packages/ui/src/components/ui/input.tsx exists and exports Input
    - packages/ui/src/components/ui/label.tsx exists and exports Label
    - packages/ui/src/components/ui/select.tsx exists and exports Select, SelectContent, SelectItem, SelectTrigger, SelectValue
    - packages/ui/src/components/ui/switch.tsx exists and exports Switch
    - packages/ui/src/components/ui/textarea.tsx exists and exports Textarea
    - packages/ui/src/components/ui/radio-group.tsx exists and exports RadioGroup, RadioGroupItem
    - packages/ui/src/components/ui/accordion.tsx exists and exports Accordion, AccordionItem, AccordionTrigger, AccordionContent
    - packages/ui/src/components/ui/alert.tsx exists and exports Alert, AlertDescription, AlertTitle
    - All component files import cn from "@/lib/utils"
  </acceptance_criteria>
  <done>10 shadcn/ui component source files exist in packages/ui/src/components/ui/ with correct exports and Radix imports</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 custom Figma-specific components</name>
  <files>
    packages/ui/src/components/figma/icon.tsx,
    packages/ui/src/components/figma/icon-button.tsx,
    packages/ui/src/components/figma/section-title.tsx,
    packages/ui/src/components/figma/type.tsx
  </files>
  <read_first>
    - packages/ui/src/components/ui/button.tsx
    - .planning/phases/13-shadcn-ui-component-migration/13-RESEARCH.md
  </read_first>
  <action>
1. Create `packages/ui/src/components/figma/` directory.

2. Create `packages/ui/src/components/figma/icon.tsx`:
   This wraps lucide-react icons. The demo app only uses 3 icon names: "plus", "info", "star". Map these (and common Figma icon names) to lucide-react equivalents. Use a name-mapping approach:

   ```tsx
   import { cn } from "@/lib/utils";
   import { Info, Plus, Star, type LucideProps } from "lucide-react";
   import type { ComponentType } from "react";

   const iconMap: Record<string, ComponentType<LucideProps>> = {
     plus: Plus,
     info: Info,
     star: Star,
   };

   export interface IconProps extends LucideProps {
     iconName: string;
     spin?: boolean;
   }

   export function Icon({ iconName, spin, className, ...props }: IconProps) {
     const LucideIcon = iconMap[iconName];
     if (!LucideIcon) return null;
     return (
       <LucideIcon
         className={cn("size-4", spin && "animate-spin", className)}
         {...props}
       />
     );
   }
   ```

3. Create `packages/ui/src/components/figma/icon-button.tsx`:
   ```tsx
   import { Button, type ButtonProps } from "@/components/ui/button";
   import { Icon, type IconProps } from "./icon";
   import { cn } from "@/lib/utils";

   interface IconButtonProps extends Omit<ButtonProps, "children"> {
     iconProps: IconProps;
     selected?: boolean;
   }

   export function IconButton({ iconProps, selected, className, ...props }: IconButtonProps) {
     return (
       <Button
         size="icon"
         variant={selected ? "default" : "ghost"}
         className={cn(className)}
         {...props}
       >
         <Icon {...iconProps} />
       </Button>
     );
   }
   ```

4. Create `packages/ui/src/components/figma/section-title.tsx`:
   ```tsx
   import { cn } from "@/lib/utils";

   interface SectionTitleProps extends React.HTMLAttributes<HTMLParagraphElement> {
     children: React.ReactNode;
   }

   export function SectionTitle({ children, className, ...props }: SectionTitleProps) {
     return (
       <p
         className={cn(
           "text-[11px] font-semibold uppercase text-muted-foreground tracking-wider",
           className
         )}
         {...props}
       >
         {children}
       </p>
     );
   }
   ```

5. Create `packages/ui/src/components/figma/type.tsx`:
   ```tsx
   import { cn } from "@/lib/utils";

   const sizeMap = {
     xsmall: "text-[11px]",
     small: "text-[12px]",
     large: "text-[13px]",
     xlarge: "text-[14px]",
   } as const;

   const weightMap = {
     normal: "font-normal",
     medium: "font-medium",
     bold: "font-semibold",
   } as const;

   interface TypeProps extends React.HTMLAttributes<HTMLParagraphElement> {
     size?: keyof typeof sizeMap;
     weight?: keyof typeof weightMap;
     children: React.ReactNode;
   }

   export function Type({
     children,
     size = "small",
     weight = "normal",
     className,
     ...props
   }: TypeProps) {
     return (
       <p
         className={cn(sizeMap[size], weightMap[weight], "font-sans", className)}
         {...props}
       >
         {children}
       </p>
     );
   }
   ```

All 4 custom components must be fully typed TypeScript (no `any`), import `cn` from `@/lib/utils`, and follow the same patterns as shadcn/ui components.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && for f in icon icon-button section-title type; do test -f "packages/ui/src/components/figma/$f.tsx" || (echo "MISSING: $f.tsx" && exit 1); done && grep -q "export function Icon" packages/ui/src/components/figma/icon.tsx && grep -q "export function IconButton" packages/ui/src/components/figma/icon-button.tsx && grep -q "export function SectionTitle" packages/ui/src/components/figma/section-title.tsx && grep -q "export function Type" packages/ui/src/components/figma/type.tsx && echo "All 4 custom components present and exported"</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/components/figma/icon.tsx exports Icon with iconName and spin props
    - packages/ui/src/components/figma/icon-button.tsx exports IconButton with iconProps and selected props
    - packages/ui/src/components/figma/section-title.tsx exports SectionTitle with children
    - packages/ui/src/components/figma/type.tsx exports Type with size and weight props
    - All 4 files import cn from "@/lib/utils"
    - No `any` types in any of the 4 files
  </acceptance_criteria>
  <done>4 custom Figma components exist with full TypeScript types, matching the API surface of the react-figma-ui originals</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn CLI -> local filesystem | CLI copies component source files into project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | T (Tampering) | shadcn CLI output | accept | CLI generates open-source component code; files are reviewed in git diff before commit |
| T-13-04 | E (Elevation) | Component props | accept | Components are UI-only in a sandboxed iframe; no privilege escalation vector |
</threat_model>

<verification>
1. All 14 component files exist in the expected directories
2. Each component file exports at least one named React component
3. All components import cn from @/lib/utils (consistent pattern)
</verification>

<success_criteria>
- 10 files in packages/ui/src/components/ui/ (shadcn standard)
- 4 files in packages/ui/src/components/figma/ (custom)
- All 14 components are valid TypeScript with proper exports
</success_criteria>

<output>
After completion, create `.planning/phases/13-shadcn-ui-component-migration/13-02-SUMMARY.md`
</output>
