---
phase: 10-vite-8-typescript-6-figma-typings
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - turbo.json
  - package.json
autonomous: true
requirements: [BUILD-01, BUILD-02, FIG-01]

must_haves:
  truths:
    - "`bun run types` works from the repo root and delegates to the figma-plugin types script"
    - "`bun run build` produces dist/plugin.js, dist/index.html, and dist/manifest.json without errors or deprecation warnings"
    - "All existing tests pass after the upgrade"
  artifacts:
    - path: "turbo.json"
      provides: "types task definition"
      contains: "\"types\""
    - path: "package.json"
      provides: "Root types script"
      contains: "\"types\":"
  key_links:
    - from: "package.json"
      to: "turbo.json"
      via: "turbo run types"
      pattern: "turbo run types"
    - from: "turbo.json"
      to: "apps/figma-plugin/package.json"
      via: "types task delegation"
      pattern: "\"types\""
---

<objective>
Add a root-level `types` script to turbo.json and root package.json so `bun run types` works from the repo root (matching the pattern of build, lint, test). Run full verification to confirm the entire upgrade is successful.

Purpose: Complete the phase by wiring the types task into the monorepo task graph and verifying all success criteria.
Output: Updated turbo.json and root package.json, verified build and type-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vite-8-typescript-6-figma-typings/10-RESEARCH.md
@.planning/phases/10-vite-8-typescript-6-figma-typings/10-01-SUMMARY.md

@turbo.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add types task to turbo.json and root package.json</name>
  <files>turbo.json, package.json</files>
  <read_first>turbo.json, package.json</read_first>
  <action>
In `turbo.json`, add a `"types"` task to the `"tasks"` object. Place it after the `"test"` task. The types task has no outputs (it only checks, doesn't produce files) and no cache (type-checking should always run fresh):

```json
"types": {
  "dependsOn": ["^build"],
  "outputs": []
}
```

The full turbo.json tasks block will be:
```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    },
    "types": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "storybook": {
      "cache": false,
      "persistent": true
    },
    "build-storybook": {
      "dependsOn": ["^build"],
      "outputs": ["storybook-static/**"]
    }
  }
}
```

In root `package.json`, add a `"types"` script to the `"scripts"` object:

```json
"types": "turbo run types"
```

Place it after the `"test:watch"` script. The full scripts block becomes:
```json
"scripts": {
  "build": "turbo run build",
  "dev": "turbo run dev",
  "lint": "turbo run lint",
  "test": "turbo run test",
  "test:watch": "turbo run test:watch",
  "types": "turbo run types"
}
```
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && grep '"types"' turbo.json package.json && bun run types 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `turbo.json` contains a `"types"` task with `"dependsOn": ["^build"]` and `"outputs": []`
- Root `package.json` contains `"types": "turbo run types"`
- `bun run types` from repo root exits 0
  </acceptance_criteria>
  <done>Root-level `bun run types` command works, delegating through Turborepo to the figma-plugin types script</done>
</task>

<task type="auto">
  <name>Task 2: Full verification of Phase 10 upgrade</name>
  <files></files>
  <read_first>apps/figma-plugin/package.json, apps/figma-plugin/vite.config.plugin.ts, apps/figma-plugin/vite.config.ui.ts</read_first>
  <action>
Run the following verification commands in sequence and confirm each passes:

1. `bun run build` — must exit 0, producing `apps/figma-plugin/dist/plugin.js`, `apps/figma-plugin/dist/index.html`, and `apps/figma-plugin/dist/manifest.json`
2. `bun run types` — must exit 0 with no TypeScript errors
3. `bun run test` — must exit 0 with all existing tests passing (7 tests across 2 packages)
4. `bun run build 2>&1 | grep -iE "deprecat|warning"` — must return no deprecation warnings from Vite or TypeScript (peer dependency warnings from Bun about vite-plugin-react-rich-svg are acceptable and expected)
5. Verify `dist/manifest.json` exists and contains valid JSON (vite-plugin-generate-file still works with Vite 8)
6. Verify `dist/index.html` is a single file with no external script/css references (vite-plugin-singlefile still works)

If any command fails:
- Build error in SVG imports: vite-plugin-react-rich-svg may be incompatible with Vite 8. Try adding `--force` or check if `vite-plugin-svgr` is needed as a replacement.
- Type errors about missing globals: Check that `"types": ["node"]` is present in the relevant tsconfig.
- manifest.json missing: vite-plugin-generate-file may need a version bump or replacement.

Do NOT modify any files in this task unless a verification failure requires a fix. If fixes are needed, document what was changed and why.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && bun run build && bun run types && bun run test && test -f apps/figma-plugin/dist/plugin.js && test -f apps/figma-plugin/dist/index.html && test -f apps/figma-plugin/dist/manifest.json && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
- `bun run build` exits 0
- `bun run types` exits 0
- `bun run test` exits 0 with 7 tests passing
- `apps/figma-plugin/dist/plugin.js` exists
- `apps/figma-plugin/dist/index.html` exists
- `apps/figma-plugin/dist/manifest.json` exists and contains valid JSON
- No Vite deprecation warnings in build output (peer dep warnings from Bun are acceptable)
  </acceptance_criteria>
  <done>All Phase 10 success criteria verified: build works with Vite 8, type-checking passes with TypeScript 6, Figma typings are at 1.124+, all tests pass, all build artifacts generated correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| turbo task graph -> package scripts | Turbo delegates to per-package scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | build output | accept | Build artifacts are local dist/ files, not deployed; no secrets in output |
</threat_model>

<verification>
This plan IS the verification step for the entire phase. All success criteria from ROADMAP Phase 10 are checked:
1. `bun run build` completes without errors using Vite 8 and Rolldown
2. `bun run types` type-checks cleanly under TypeScript 6.0
3. @figma/plugin-typings is at 1.124 and Figma API symbols resolve
4. Both Vite configs run without deprecation errors
</verification>

<success_criteria>
- `bun run types` works from repo root via Turborepo
- Full build, type-check, and test suite pass after all Phase 10 changes
- All three build artifacts (plugin.js, index.html, manifest.json) are present in dist/
</success_criteria>

<output>
After completion, create `.planning/phases/10-vite-8-typescript-6-figma-typings/10-02-SUMMARY.md`
</output>
