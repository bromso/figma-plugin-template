---
phase: 11-react-19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - apps/storybook/package.json
  - bun.lock
autonomous: true
requirements:
  - FW-01
must_haves:
  truths:
    - "react and react-dom are at version 19.x in packages/ui and apps/storybook"
    - "@types/react and @types/react-dom are at React 19-compatible versions in packages/ui"
    - "bun run types passes with no new TypeScript errors"
    - "bun run test passes all existing tests"
    - "The plugin UI renders without console errors"
  artifacts:
    - path: "packages/ui/package.json"
      provides: "React 19 runtime and type dependencies"
      contains: "19.2"
    - path: "apps/storybook/package.json"
      provides: "React 19 for Storybook renderer"
      contains: "19.2"
    - path: "bun.lock"
      provides: "Resolved React 19.x packages"
  key_links:
    - from: "packages/ui/package.json"
      to: "bun.lock"
      via: "bun install resolves versions"
      pattern: "react.*19"
    - from: "apps/storybook/package.json"
      to: "bun.lock"
      via: "bun install resolves versions"
      pattern: "react.*19"
---

<objective>
Upgrade React from 18.2 to 19.x across the monorepo, including type definitions, and verify that all existing code compiles and tests pass.

Purpose: React 19 is required before Storybook 10 (Phase 14) and keeps the template on the current React major version.
Output: Updated package.json files with React 19.x dependencies, clean type-check, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-react-19/11-RESEARCH.md

@packages/ui/package.json
@apps/storybook/package.json
</context>

<interfaces>
<!-- No new interfaces created. This plan modifies dependency versions only. -->

From packages/ui/package.json (current state — to be modified):
```json
"dependencies": {
  "react": "^18.2.0",
  "react-dom": "^18.2.0"
},
"devDependencies": {
  "@types/react": "^18.2.0",
  "@types/react-dom": "^18.2.0"
}
```

From apps/storybook/package.json (current state — to be modified):
```json
"devDependencies": {
  "react": "^18.2.0",
  "react-dom": "^18.2.0"
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bump React dependencies to 19.x</name>
  <files>packages/ui/package.json, apps/storybook/package.json, bun.lock</files>
  <read_first>
    packages/ui/package.json
    apps/storybook/package.json
    .planning/phases/11-react-19/11-RESEARCH.md
  </read_first>
  <action>
Update React dependency versions in both workspace packages. Do NOT touch any other dependencies.

In `packages/ui/package.json`:
- Change `"react": "^18.2.0"` to `"react": "^19.2.5"` in dependencies
- Change `"react-dom": "^18.2.0"` to `"react-dom": "^19.2.5"` in dependencies
- Change `"@types/react": "^18.2.0"` to `"@types/react": "^19.2.14"` in devDependencies
- Change `"@types/react-dom": "^18.2.0"` to `"@types/react-dom": "^19.2.3"` in devDependencies

In `apps/storybook/package.json`:
- Change `"react": "^18.2.0"` to `"react": "^19.2.5"` in devDependencies
- Change `"react-dom": "^18.2.0"` to `"react-dom": "^19.2.5"` in devDependencies

After editing both files, run `bun install` from the workspace root to update `bun.lock`.

Then verify the lockfile resolved correctly:
- Run `bun pm ls 2>/dev/null | grep react` and confirm all react entries show 19.x versions
- Confirm there is only ONE resolved version of react (no duplicate 18.x + 19.x)

Do NOT change `@vitejs/plugin-react`, `@testing-library/react`, or any other dependency. They are already compatible per the research document.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && grep -q '"react": "\\^19' packages/ui/package.json && grep -q '"react-dom": "\\^19' packages/ui/package.json && grep -q '"@types/react": "\\^19' packages/ui/package.json && grep -q '"@types/react-dom": "\\^19' packages/ui/package.json && grep -q '"react": "\\^19' apps/storybook/package.json && grep -q '"react-dom": "\\^19' apps/storybook/package.json && echo "PASS: All React deps bumped to 19.x"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"react": "\\^19' packages/ui/package.json` matches
    - `grep '"react-dom": "\\^19' packages/ui/package.json` matches
    - `grep '"@types/react": "\\^19' packages/ui/package.json` matches
    - `grep '"@types/react-dom": "\\^19' packages/ui/package.json` matches
    - `grep '"react": "\\^19' apps/storybook/package.json` matches
    - `grep '"react-dom": "\\^19' apps/storybook/package.json` matches
    - `bun install` exits 0 with no errors
    - No `18.2` react entries remain in either package.json
  </acceptance_criteria>
  <done>Both package.json files declare React 19.x dependencies, bun.lock is updated with resolved 19.x versions, no duplicate React versions installed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify type-checking, tests, and build</name>
  <files>packages/ui/package.json, apps/storybook/package.json</files>
  <read_first>
    packages/ui/package.json
    apps/storybook/package.json
  </read_first>
  <action>
Run the full verification suite to confirm React 19 works correctly with the existing codebase.

Step 1 — Type-check:
Run `bun run types` from the workspace root. This runs `tsc --noEmit` across all packages via Turborepo. Must exit 0 with no errors.

Step 2 — Tests:
Run `bun run test` from the workspace root. All 7 existing tests (across @repo/ui and @repo/common) must pass. Pay attention to:
- `packages/ui/src/__tests__/App.test.tsx` — this renders the React app and confirms React 19 runtime works
- Any "Invalid hook call" errors would indicate a duplicate React version problem (see Pitfall 4 in research)

Step 3 — Build:
Run `bun run build` from the workspace root. Must produce `apps/figma-plugin/dist/plugin.js` and `apps/figma-plugin/dist/index.html` without errors.

If any step fails, diagnose and fix. The research document (11-RESEARCH.md) lists common pitfalls and their solutions. Most likely issues:
- Type errors: verify `@types/react` is at `^19.2.14`
- Hook errors at runtime: verify no duplicate React versions via `bun pm ls | grep react`
- Build errors: verify `bun install` was run after the version bump
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && bun run types && bun run test && bun run build && echo "PASS: All checks green"</automated>
  </verify>
  <acceptance_criteria>
    - `bun run types` exits 0 (no TypeScript errors)
    - `bun run test` exits 0 (all 7 tests pass)
    - `bun run build` exits 0 (dist/plugin.js and dist/index.html produced)
    - No "Invalid hook call" errors in test output
    - No React deprecation warnings in test or build output
  </acceptance_criteria>
  <done>TypeScript type-checking passes, all existing tests pass, production build succeeds — React 19 upgrade is fully verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This phase changes dependency versions only — no new code, no new endpoints, no new input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | npm registry packages | accept | Bun lockfile pins resolved versions with integrity hashes. Standard supply-chain trust model — same as all prior phases. |
</threat_model>

<verification>
1. `grep '"react": "\\^19' packages/ui/package.json` — confirms version bump
2. `grep '"react": "\\^19' apps/storybook/package.json` — confirms version bump
3. `bun run types` — TypeScript compiles cleanly with React 19 types
4. `bun run test` — all existing tests pass
5. `bun run build` — production build succeeds
</verification>

<success_criteria>
- react and react-dom are ^19.2.5 in packages/ui (dependencies) and apps/storybook (devDependencies)
- @types/react is ^19.2.14 and @types/react-dom is ^19.2.3 in packages/ui devDependencies
- bun.lock resolves a single React 19.x version (no duplicates)
- bun run types exits 0
- bun run test exits 0 (all 7 tests pass)
- bun run build exits 0 (dist/ output produced)
</success_criteria>

<output>
After completion, create `.planning/phases/11-react-19/11-01-SUMMARY.md`
</output>
