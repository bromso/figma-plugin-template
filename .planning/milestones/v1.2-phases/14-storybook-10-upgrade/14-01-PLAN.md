---
phase: 14-storybook-10-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/storybook/package.json
  - apps/storybook/.storybook/main.ts
  - apps/storybook/.storybook/preview.ts
autonomous: true
requirements: [FW-02]

must_haves:
  truths:
    - "Storybook 10.3.5 packages are installed and addon-essentials is removed"
    - "main.ts addons array references addon-docs instead of addon-essentials"
    - "Config files are pure ESM with no require() calls"
    - "build-storybook compiles without module resolution errors"
  artifacts:
    - path: "apps/storybook/package.json"
      provides: "Storybook 10.3.5 dependencies"
      contains: "\"storybook\": \"10.3.5\""
    - path: "apps/storybook/.storybook/main.ts"
      provides: "ESM Storybook config with addon-docs"
      contains: "@storybook/addon-docs"
  key_links:
    - from: "apps/storybook/.storybook/main.ts"
      to: "@storybook/addon-docs"
      via: "addons array"
      pattern: "addon-docs"
---

<objective>
Upgrade Storybook packages from 8.6.18 to 10.3.5 and update configuration files for ESM-only v10.

Purpose: Establish the Storybook 10 runtime so that story rewrites (Plan 02) can be validated against the new framework version.
Output: Working Storybook 10 installation with updated main.ts and preview.ts config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-storybook-10-upgrade/14-RESEARCH.md

@apps/storybook/package.json
@apps/storybook/.storybook/main.ts
@apps/storybook/.storybook/preview.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Storybook packages to 10.3.5</name>
  <files>apps/storybook/package.json</files>
  <read_first>apps/storybook/package.json</read_first>
  <action>
From the `apps/storybook/` directory, run:

```bash
cd apps/storybook && bun remove @storybook/addon-essentials && bun add -d storybook@10.3.5 @storybook/react@10.3.5 @storybook/react-vite@10.3.5 @storybook/addon-docs@10.3.5
```

After install, verify package.json:
- `storybook` is `10.3.5`
- `@storybook/react` is `10.3.5`
- `@storybook/react-vite` is `10.3.5`
- `@storybook/addon-docs` is `10.3.5`
- `@storybook/addon-essentials` does NOT appear anywhere in package.json

Keep existing deps: `@repo/ui`, `react`, `react-dom`, `vite`, `@vitejs/plugin-react`.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && grep -c "addon-essentials" apps/storybook/package.json | grep -q "^0$" && grep -q '"storybook": "10.3.5"' apps/storybook/package.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "storybook" apps/storybook/package.json shows version 10.3.5
    - grep "addon-essentials" apps/storybook/package.json returns 0 matches
    - grep "addon-docs" apps/storybook/package.json returns 1 match with version 10.3.5
  </acceptance_criteria>
  <done>Storybook 10.3.5 packages installed; addon-essentials removed; addon-docs added</done>
</task>

<task type="auto">
  <name>Task 2: Update Storybook config files for v10</name>
  <files>apps/storybook/.storybook/main.ts, apps/storybook/.storybook/preview.ts</files>
  <read_first>apps/storybook/.storybook/main.ts, apps/storybook/.storybook/preview.ts</read_first>
  <action>
Update `apps/storybook/.storybook/main.ts`:
- Change `addons: ['@storybook/addon-essentials']` to `addons: ['@storybook/addon-docs']`
- Keep everything else unchanged (framework, stories glob, viteFinal are all valid for v10)

Review `apps/storybook/.storybook/preview.ts`:
- The `Preview` type import from `@storybook/react-vite` should work in v10. If it causes a TS error during build, change to `import type { Preview } from 'storybook'`.
- The `tags: ['autodocs']` global is correct for v10.
- The `parameters.viewport` config is valid (viewport is built into core in v10).
- The `import '@repo/ui'` side-effect import for Tailwind CSS loading stays.

After updating, run:
```bash
cd /Users/jonasbroms/Sites/figma-plugin-template && turbo run build-storybook
```

This will fail on story TypeScript errors (expected — stories still use old react-figma-ui APIs). The goal is to verify Storybook 10 framework initializes correctly. If the error is about `addon-essentials` or module resolution, that is a config problem to fix. If the errors are about component props in story files, that confirms config is correct and stories need rewriting (Plan 02).

If build-storybook fails due to story TS errors, that is acceptable for this task. Verify by checking the error output references story files (e.g., `Button.stories.tsx`, `Select.stories.tsx`) rather than config files.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && grep -q "addon-docs" apps/storybook/.storybook/main.ts && ! grep -q "addon-essentials" apps/storybook/.storybook/main.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "addon-docs" apps/storybook/.storybook/main.ts returns a match
    - grep "addon-essentials" apps/storybook/.storybook/main.ts returns 0 matches
    - grep "require(" apps/storybook/.storybook/main.ts returns 0 matches (ESM only)
    - grep "require(" apps/storybook/.storybook/preview.ts returns 0 matches (ESM only)
  </acceptance_criteria>
  <done>main.ts uses addon-docs; preview.ts is valid for v10; no CommonJS require() in config files; build-storybook errors (if any) are story-level not config-level</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> project | Package versions must match expected (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | npm packages | accept | Dev dependency only; not shipped to end users; Bun lockfile pins exact versions |
</threat_model>

<verification>
- Storybook 10.3.5 packages are in package.json
- addon-essentials is fully removed (package.json + main.ts)
- addon-docs is in both package.json and main.ts addons array
- No CommonJS require() in .storybook/ config files
</verification>

<success_criteria>
Storybook 10.3.5 is installed and configured. Config files are pure ESM. The framework initializes correctly (any remaining errors are story-level TypeScript issues, not Storybook config issues).
</success_criteria>

<output>
After completion, create `.planning/phases/14-storybook-10-upgrade/14-01-SUMMARY.md`
</output>
