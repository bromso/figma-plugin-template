---
phase: 15-full-stack-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VER-01, VER-02]

must_haves:
  truths:
    - "All 7 tests across 2 packages pass with zero failures and zero skips"
    - "dist/plugin.js exists and is a valid JavaScript bundle"
    - "dist/index.html exists as a single-file HTML with all CSS/JS inlined — no external references"
  artifacts:
    - path: "apps/figma-plugin/dist/plugin.js"
      provides: "Compiled plugin sandbox code"
    - path: "apps/figma-plugin/dist/index.html"
      provides: "Single-file UI HTML with inlined assets"
  key_links:
    - from: "packages/ui/src/__tests__/exports.test.ts"
      to: "packages/ui/src/index.ts"
      via: "import validation"
      pattern: "import.*@repo/ui"
    - from: "packages/common/src/__tests__/networkSides.test.ts"
      to: "packages/common/src/networkSides.ts"
      via: "network event type checks"
      pattern: "import.*networkSides"
---

<objective>
Run the full test suite and production build to confirm all upgrades (Vite 8, TypeScript 6, React 19, Tailwind CSS 4, shadcn/ui, Storybook 10) have not broken core functionality.

Purpose: Verify VER-01 (tests pass) and VER-02 (valid single-file build output) after 5 phases of dependency upgrades.
Output: Passing test suite and verified production build artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/ui/src/__tests__/exports.test.ts
@packages/ui/src/__tests__/App.test.tsx
@packages/common/src/__tests__/networkSides.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify all pass</name>
  <files></files>
  <read_first>
    - packages/ui/src/__tests__/exports.test.ts
    - packages/ui/src/__tests__/App.test.tsx
    - packages/common/src/__tests__/networkSides.test.ts
  </read_first>
  <action>
Run `bun run test` from the repo root. This executes Vitest across all workspace packages via Turborepo.

Expected: 7 tests across 2 packages (packages/ui and packages/common) all pass with zero failures and zero skips.

If any tests fail:
1. Read the failing test file and the source file it imports.
2. Diagnose whether the failure is due to a Phase 10-14 upgrade (e.g., changed export names after shadcn/ui migration, React 19 API changes, TypeScript 6 stricter checks).
3. Fix the source code or test to restore green — do NOT delete or skip tests.
4. Re-run `bun run test` to confirm the fix.

Also run `bun run lint` to confirm Biome passes with no errors.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && bun run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test` exits with code 0
    - Output shows 7 tests passed, 0 failed, 0 skipped
    - `bun run lint` exits with code 0
  </acceptance_criteria>
  <done>All 7 tests pass across packages/ui and packages/common. Lint passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run production build and verify single-file output</name>
  <files></files>
  <read_first>
    - apps/figma-plugin/vite.config.ui.ts
    - apps/figma-plugin/vite.config.plugin.ts
  </read_first>
  <action>
Run `bun run build` from the repo root. This executes the Turborepo build pipeline producing output in `apps/figma-plugin/dist/`.

After the build completes, verify:

1. `dist/plugin.js` exists and is non-empty (the Figma sandbox bundle).
2. `dist/index.html` exists and is non-empty (the single-file UI).
3. `dist/manifest.json` exists (the Figma plugin manifest).
4. `dist/index.html` contains NO external references — grep for `<link rel="stylesheet" href=` and `<script src=` patterns that reference external files. The file must be fully self-contained (all CSS and JS inlined via vite-plugin-singlefile).
5. `dist/index.html` contains Tailwind CSS utility classes inlined in a `<style>` tag (not an external .css file).

If the build fails:
1. Read the error output carefully.
2. Check if vite-plugin-singlefile or vite-plugin-react-rich-svg have compatibility issues with Vite 8.
3. Fix configuration issues and re-run.

If external references found in index.html:
1. Check vite.config.ui.ts singlefile plugin configuration.
2. Ensure all assets are inlined.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && bun run build 2>&1 | tail -10 && echo "---DIST---" && ls -la apps/figma-plugin/dist/ && echo "---EXTERNAL-REFS---" && grep -cE '(href|src)="[^"]*\.(css|js)"' apps/figma-plugin/dist/index.html || echo "0 external refs (good)"</automated>
  </verify>
  <acceptance_criteria>
    - `bun run build` exits with code 0
    - `apps/figma-plugin/dist/plugin.js` exists and is > 0 bytes
    - `apps/figma-plugin/dist/index.html` exists and is > 0 bytes
    - `apps/figma-plugin/dist/manifest.json` exists
    - `dist/index.html` has 0 external CSS/JS references (fully inlined)
  </acceptance_criteria>
  <done>Production build succeeds. dist/ contains plugin.js, index.html (single-file, fully inlined), and manifest.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output -> Figma sandbox | dist/plugin.js runs in Figma's restricted sandbox |
| Build output -> Figma iframe | dist/index.html loads in Figma's iframe with CSP restrictions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | dist/ build artifacts | accept | Build artifacts are generated locally and loaded into Figma manually; no supply chain risk beyond npm dependencies already audited |
| T-15-02 | Information Disclosure | index.html inlined JS | accept | Plugin UI code is client-side only, no secrets; Figma iframe sandboxing prevents cross-origin leaks |
</threat_model>

<verification>
- `bun run test` exits 0 with all tests passing
- `bun run build` exits 0 with valid dist/ output
- `bun run lint` exits 0
- dist/index.html is a self-contained single file
</verification>

<success_criteria>
VER-01: All 7 tests pass with 0 failures, 0 skips
VER-02: dist/plugin.js + dist/index.html exist as valid single-file outputs with no external references
</success_criteria>

<output>
After completion, create `.planning/phases/15-full-stack-verification/15-01-SUMMARY.md`
</output>
