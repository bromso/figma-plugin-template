---
phase: 15-full-stack-verification
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: []
autonomous: false
requirements: [VER-03, VER-04]

must_haves:
  truths:
    - "Storybook build completes without errors producing a static site"
    - "All 14 component stories render in Storybook with working Controls and Autodocs"
    - "The plugin loaded in Figma renders a UI visually consistent with native Figma plugin appearance"
  artifacts:
    - path: "apps/storybook/storybook-static/index.html"
      provides: "Built Storybook static site entry"
  key_links:
    - from: "apps/storybook/src/stories/*.stories.tsx"
      to: "packages/ui/src/components/ui/*.tsx"
      via: "component imports"
      pattern: "import.*@repo/ui"
    - from: "apps/figma-plugin/dist/index.html"
      to: "packages/ui/src/app.tsx"
      via: "built UI bundle"
      pattern: "inlined in single-file HTML"
---

<objective>
Verify Storybook builds and renders all shadcn/ui component stories, then confirm the plugin UI looks correct in Figma.

Purpose: Verify VER-03 (Storybook stories render) and VER-04 (Figma visual parity) to close out the v1.2 milestone.
Output: Verified Storybook build and Figma visual confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-full-stack-verification/15-01-SUMMARY.md
@apps/storybook/.storybook/main.ts
@apps/storybook/.storybook/preview.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Storybook and verify all stories compile</name>
  <files></files>
  <read_first>
    - apps/storybook/.storybook/main.ts
    - apps/storybook/package.json
  </read_first>
  <action>
Run `turbo run build-storybook` from the repo root to build the static Storybook site.

This compiles all 14 component stories in `apps/storybook/src/stories/`:
- Accordion.stories.tsx (Disclosure equivalent)
- Alert.stories.tsx (OnboardingTip equivalent)
- Button.stories.tsx
- Checkbox.stories.tsx
- Icon.stories.tsx
- IconButton.stories.tsx
- Input.stories.tsx
- Label.stories.tsx
- RadioGroup.stories.tsx (Radio equivalent)
- SectionTitle.stories.tsx
- Select.stories.tsx
- Switch.stories.tsx
- Textarea.stories.tsx
- Type.stories.tsx

After build completes, verify:
1. `apps/storybook/storybook-static/` directory exists with an `index.html`.
2. The build output mentions all 14 stories were processed without errors.
3. No TypeScript or import errors in the build log.

If the build fails:
1. Read the error — likely a story file importing a component that was renamed or moved during Phase 13/14.
2. Fix the import path or story API to match Storybook 10 + shadcn/ui component names.
3. Re-run the build.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && turbo run build-storybook 2>&1 | tail -20 && echo "---STATIC---" && ls apps/storybook/storybook-static/index.html 2>/dev/null && echo "BUILD OK" || echo "BUILD FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `turbo run build-storybook` exits with code 0
    - `apps/storybook/storybook-static/index.html` exists
    - Build log shows no TypeScript errors or missing module warnings
    - All 14 story files compiled without errors
  </acceptance_criteria>
  <done>Storybook static build completes successfully with all 14 component stories compiled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification in Storybook and Figma</name>
  <files></files>
  <action>
This is a human verification checkpoint. No automated action — the user visually inspects Storybook and Figma.

Before presenting the checkpoint, start the Storybook dev server with `bun run storybook` so it is ready for the user to inspect at http://localhost:6006.
  </action>
  <what-built>
Full-stack verification of all v1.2 upgrades:
- All tests pass (Plan 01)
- Production build produces valid single-file output (Plan 01)
- Storybook builds with all 14 shadcn/ui component stories (Task 1)
  </what-built>
  <how-to-verify>
**Storybook verification (VER-03):**
1. Run `bun run storybook` from the repo root
2. Open http://localhost:6006 in your browser
3. Verify each of the 14 component stories renders:
   - Button, Checkbox, Input, Label, Select, Switch, Textarea
   - RadioGroup, Icon, IconButton, Accordion, SectionTitle, Alert, Type
4. Click on a few stories and confirm Controls panel works (change props, see updates)
5. Click "Docs" tab on any component to confirm Autodocs page loads

**Figma verification (VER-04):**
1. Open Figma Desktop
2. Go to Plugins > Development > Import plugin from manifest
3. Select `apps/figma-plugin/dist/manifest.json`
4. Run the plugin
5. Confirm the UI renders and looks visually consistent with native Figma plugin appearance (proper colors, typography, spacing matching Figma's design system)
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint — no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 14 Storybook stories render without errors
    - User confirms Controls panel is interactive
    - User confirms Autodocs pages load
    - User confirms Figma plugin UI renders with native appearance
  </acceptance_criteria>
  <done>User approves both Storybook rendering and Figma visual appearance.</done>
  <resume-signal>Type "approved" if both Storybook and Figma look correct, or describe any visual issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storybook dev server -> browser | Local dev server, no production exposure |
| dist/ -> Figma runtime | Plugin loads in Figma's sandboxed environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | Storybook static build | accept | Storybook is a dev tool, not deployed to production; contains no secrets |
| T-15-04 | Tampering | Figma plugin manifest | accept | manifest.json is generated at build time from figma.manifest.ts; loaded locally by developer |
</threat_model>

<verification>
- `turbo run build-storybook` exits 0
- Storybook static site exists at apps/storybook/storybook-static/
- Human confirms all 14 stories render with Controls and Autodocs
- Human confirms plugin UI looks correct in Figma
</verification>

<success_criteria>
VER-03: All Storybook stories render with shadcn/ui components, Controls work, Autodocs load
VER-04: Plugin in Figma renders UI visually consistent with native Figma plugin appearance
</success_criteria>

<output>
After completion, create `.planning/phases/15-full-stack-verification/15-02-SUMMARY.md`
</output>
