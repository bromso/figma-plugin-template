---
phase: 03-build-pipeline-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/figma-plugin/package.json
  - bun.lock
autonomous: true
requirements:
  - BUILD-03
  - BUILD-04
  - BUILD-05
must_haves:
  truths:
    - "Vite 6 is installed and all plugins load without peer dependency errors"
    - "turbo run build produces dist/plugin.js, dist/index.html, and dist/manifest.json"
    - "dist/index.html has all assets inlined (no external src= or href= attributes)"
    - "bun run dev starts both UI and plugin watch processes without errors"
  artifacts:
    - path: "apps/figma-plugin/package.json"
      provides: "Vite 6 dependency declaration"
      contains: '"vite": "^6.0.0"'
    - path: "apps/figma-plugin/dist/plugin.js"
      provides: "Built plugin code for Figma sandbox"
    - path: "apps/figma-plugin/dist/index.html"
      provides: "Fully inlined single-file UI for Figma iframe"
    - path: "apps/figma-plugin/dist/manifest.json"
      provides: "Generated Figma plugin manifest"
  key_links:
    - from: "apps/figma-plugin/vite.config.ui.ts"
      to: "vite-plugin-react-rich-svg"
      via: "ESM import (requires Vite 6 native ESM config loader)"
      pattern: "import richSvg from"
    - from: "apps/figma-plugin/vite.config.plugin.ts"
      to: "vite-plugin-generate-file"
      via: "ESM import for manifest generation"
      pattern: "import generateFile from"
---

<objective>
Upgrade Vite from v5 to v6 in the figma-plugin app and verify both build configs produce valid single-file output.

Purpose: The build is currently broken because Vite 5's CJS config loader cannot require() the ESM-only vite-plugin-react-rich-svg. Vite 6 uses native ESM config loading, which resolves this. All plugins already declare Vite 6 peer compatibility.

Output: Working build pipeline producing dist/plugin.js, dist/index.html (fully inlined), and dist/manifest.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-build-pipeline-verification/03-RESEARCH.md
@apps/figma-plugin/package.json
@apps/figma-plugin/vite.config.ui.ts
@apps/figma-plugin/vite.config.plugin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Vite to v6 and refresh lockfile</name>
  <files>apps/figma-plugin/package.json, bun.lock</files>
  <read_first>
    - apps/figma-plugin/package.json
    - .planning/phases/03-build-pipeline-verification/03-RESEARCH.md (Standard Stack section)
  </read_first>
  <action>
In apps/figma-plugin/package.json, change the vite version specifier from "^5.0.11" to "^6.0.0" in devDependencies. This is the ONLY change needed in package.json.

Do NOT upgrade @vitejs/plugin-react — keep it at "^4.0.0". The v4.7.0 currently installed supports Vite 6. Upgrading to v6.x would require Vite 8 (per BUILD-03 research).

Do NOT change any other dependency versions. All plugins (vite-plugin-singlefile, vite-plugin-react-rich-svg, vite-plugin-generate-file) already support Vite 6 via their peerDependencies.

After editing package.json, run from the repo root:
```bash
cd /Users/jonasbroms/Sites/figma-plugin-template && bun install
```

Verify the install completes with exit code 0 and no peer dependency errors in output.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && grep '"vite"' apps/figma-plugin/package.json | grep -q '6.0.0' && echo "PASS: vite version updated" || echo "FAIL: vite version not updated"</automated>
  </verify>
  <acceptance_criteria>
    - apps/figma-plugin/package.json contains "vite": "^6.0.0" in devDependencies
    - @vitejs/plugin-react remains at "^4.0.0" (NOT upgraded)
    - bun install exits 0 with no peer dependency errors
    - bun.lock is updated to reflect the new vite version
  </acceptance_criteria>
  <done>Vite 6 declared in package.json and installed via bun without errors</done>
</task>

<task type="auto">
  <name>Task 2: Verify build pipeline and dev mode</name>
  <files>apps/figma-plugin/dist/plugin.js, apps/figma-plugin/dist/index.html, apps/figma-plugin/dist/manifest.json</files>
  <read_first>
    - apps/figma-plugin/vite.config.ui.ts
    - apps/figma-plugin/vite.config.plugin.ts
    - .planning/phases/03-build-pipeline-verification/03-RESEARCH.md (Validation Architecture section)
  </read_first>
  <action>
Run the full build from the repo root and verify all outputs:

```bash
cd /Users/jonasbroms/Sites/figma-plugin-template && bun run build
```

This executes turbo run build, which runs the app's build:ui then build:plugin scripts.

After the build completes successfully, verify all three output files exist:
1. apps/figma-plugin/dist/index.html — the fully inlined UI HTML
2. apps/figma-plugin/dist/plugin.js — the plugin sandbox code
3. apps/figma-plugin/dist/manifest.json — the generated Figma manifest

Verify the singlefile constraint on index.html: check that no external src= or href= attributes remain (all scripts, styles, fonts, and images must be inlined). Run:
```bash
grep -cE '(src|href)=' apps/figma-plugin/dist/index.html
```
Expected: 0 matches. If any matches appear, inspect them — data: URIs and inline script tags are acceptable, but external file references are not.

Verify manifest.json is valid JSON:
```bash
cat apps/figma-plugin/dist/manifest.json | python3 -m json.tool > /dev/null && echo "Valid JSON"
```

Verify dev mode starts correctly by running bun run dev for a few seconds and checking both watchers initialize:
```bash
timeout 15 bun run dev 2>&1 || true
```
Look for both vite build --watch processes starting (one for UI, one for plugin). The timeout will kill the process — that is expected. Success means both watchers start without errors.

If any step fails, diagnose using the pitfalls documented in 03-RESEARCH.md and fix accordingly. The Vite configs themselves should not need changes — only the version bump should be required.
  </action>
  <verify>
    <automated>cd /Users/jonasbroms/Sites/figma-plugin-template && bun run build && test -f apps/figma-plugin/dist/index.html && test -f apps/figma-plugin/dist/plugin.js && test -f apps/figma-plugin/dist/manifest.json && echo "PASS: all build outputs present" || echo "FAIL: missing build outputs"</automated>
  </verify>
  <acceptance_criteria>
    - bun run build (turbo run build) exits 0 with no errors
    - apps/figma-plugin/dist/index.html exists and contains no external src= or href= attributes (fully inlined)
    - apps/figma-plugin/dist/plugin.js exists
    - apps/figma-plugin/dist/manifest.json exists and contains valid JSON
    - bun run dev starts both UI and plugin watch processes without immediate errors
  </acceptance_criteria>
  <done>Build pipeline produces all three expected output files with fully inlined UI HTML, and dev mode starts both watchers successfully</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries introduced in this phase. This is a build tooling version upgrade with no runtime, network, or authentication changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm dependency supply chain | accept | Only upgrading vite (well-known package); no new dependencies added; bun.lock provides integrity hashes |
</threat_model>

<verification>
1. `bun run build` exits 0 — both Vite configs process without ESM loading errors
2. `ls apps/figma-plugin/dist/` shows index.html, plugin.js, manifest.json
3. `grep -cE '(src|href)=' apps/figma-plugin/dist/index.html` returns 0 (singlefile constraint met)
4. `bun run dev` starts both watch processes (verified via timeout check)
5. `grep '"vite"' apps/figma-plugin/package.json` shows "^6.0.0"
</verification>

<success_criteria>
- BUILD-03: Vite 6 installed with all plugins compatible (no peer dep errors)
- BUILD-04: turbo run build produces dist/plugin.js and dist/index.html (fully inlined) plus dist/manifest.json
- BUILD-05: bun run dev starts parallel watch mode for both plugin and UI builds
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-pipeline-verification/03-01-SUMMARY.md`
</output>
